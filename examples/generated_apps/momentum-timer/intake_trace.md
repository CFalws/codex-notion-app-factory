# Intake Trace

## Event

The request was captured, normalized into a local snapshot, and then used to generate the app artifacts in this folder.

## Purpose

This file exists to make the provenance explicit:

structured request -> local request snapshot -> generated artifacts -> implemented app
