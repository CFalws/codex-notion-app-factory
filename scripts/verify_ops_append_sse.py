#!/usr/bin/env python3
from __future__ import annotations

from pathlib import Path
import sys


ROOT = Path(__file__).resolve().parents[1]


def read(relative_path: str) -> str:
    return (ROOT / relative_path).read_text(encoding="utf-8")


def require(content: str, needle: str, *, label: str) -> None:
    if needle not in content:
        raise AssertionError(f"missing {label}: {needle}")


def require_absent(content: str, needle: str, *, label: str) -> None:
    if needle in content:
        raise AssertionError(f"unexpected {label}: {needle}")


def main() -> int:
    app_js = read("examples/generated_apps/codex-ops-console/web/app.js")
    api_js = read("examples/generated_apps/codex-ops-console/web/ops-api.js")
    conversations_js = read("examples/generated_apps/codex-ops-console/web/ops-conversations.js")
    jobs_js = read("examples/generated_apps/codex-ops-console/web/ops-jobs.js")
    render_js = read("examples/generated_apps/codex-ops-console/web/ops-render.js")
    store_js = read("examples/generated_apps/codex-ops-console/web/ops-store.js")
    api_runtime_context_py = read("src/codex_factory_runtime/api_runtime_context.py")
    api_routes_py = read("src/codex_factory_runtime/api_routes.py")
    deployed_console_sh = read("scripts/verify_deployed_console.sh")
    deployed_workspace_py = read("scripts/verify_deployed_workspace_gate.py")
    styles_css = read("examples/generated_apps/codex-ops-console/web/styles.css")
    index_html = read("examples/generated_apps/codex-ops-console/web/index.html")
    autonomy_detail_index = index_html.index('id="autonomy-detail"')
    thread_scroller_index = index_html.index('id="thread-scroller"')
    footer_dock_index = index_html.index('id="conversation-footer-dock"')
    composer_shell_index = index_html.index('class="composer-shell"')
    session_strip_index = index_html.index('id="session-strip"')
    draft_status_index = index_html.index('id="draft-status"')
    composer_footer_index = index_html.index('class="composer-footer"')
    sidebar_index = index_html.index('id="nav-sheet"')
    main_stage_index = index_html.index('<section class="main-stage panel" data-primary-surface="conversation">')
    secondary_panel_index = index_html.index('id="secondary-panel"')
    if not (secondary_panel_index < autonomy_detail_index):
        raise AssertionError("detailed autonomy summary is no longer reachable through the secondary panel")
    if not (thread_scroller_index < footer_dock_index < composer_shell_index):
        raise AssertionError("thread scroller and footer dock order no longer preserves the conversation workspace")
    if not (composer_shell_index < session_strip_index < draft_status_index < composer_footer_index):
        raise AssertionError("composer-adjacent activity bar no longer sits inside the composer shell ahead of footer actions")
    if not (sidebar_index < main_stage_index < secondary_panel_index):
        raise AssertionError("desktop workspace no longer uses sidebar, main stage, then secondary panel order")

    require(api_js, "/api/internal/conversations/${conversationId}/append-stream", label="append SSE URL builder")
    require(api_js, 'url.searchParams.set("after", String(afterAppendId));', label="append SSE resume cursor query")
    require(conversations_js, "new window.EventSource(", label="EventSource wiring")
    require(conversations_js, "internalConversationAppendStreamUrl(conversationId, { afterAppendId: resumeCursor })", label="EventSource resume URL builder")
    require(conversations_js, 'addEventListener("session.bootstrap"', label="session bootstrap event listener")
    require(conversations_js, 'addEventListener("conversation.append"', label="append event listener")
    require(conversations_js, "appendEnvelope.conversation_id !== activeConversationId", label="cross-conversation guard")
    require(conversations_js, "appendId <= Number(state.appendStream?.lastAppendId || 0)", label="append_id dedupe")
    require(conversations_js, 'state.appendStream.status = isResumeAttempt ? "reconnecting" : "connecting";', label="connecting state")
    require(conversations_js, 'state.appendStream.status = "live"', label="live state")
    require(conversations_js, 'state.appendStream.status = "reconnecting"', label="reconnecting state")
    require(conversations_js, 'state.appendStream.attachMode = isResumeAttempt ? "sse-resume" : "awaiting-bootstrap";', label="attach mode awaiting bootstrap state")
    require(conversations_js, 'state.appendStream.attachMode = "snapshot-fallback"', label="attach mode snapshot fallback state")
    require(conversations_js, 'state.appendStream.attachMode = "sse-resume"', label="attach mode resume state")
    require(conversations_js, 'state.appendStream.attachMode = String(payload.attach_mode || (isResumeAttempt ? "sse-resume" : "sse-bootstrap"));', label="attach mode bootstrap state")
    require(conversations_js, 'state.appendStream.bootstrapVersion = String(payload.version || "")', label="bootstrap version state")
    require(conversations_js, 'state.appendStream.resumeMode = "awaiting-resume"', label="resume awaiting state")
    require(conversations_js, 'state.appendStream.resumeMode = isResumeAttempt ? "resuming" : "bootstrap";', label="resume active state")
    require(conversations_js, 'state.appendStream.resumeMode = isResumeAttempt ? "resumed" : "bootstrap";', label="resume completed state")
    require(conversations_js, 'state.appendStream.resumeMode = "fallback"', label="resume fallback state")
    require(conversations_js, "const restoreSelection =", label="saved selected-thread restore selection")
    require(conversations_js, 'state.currentConversationId = conversationId;', label="restore seeds current conversation")
    require(conversations_js, 'state.appendStream.lastRenderSource = "snapshot";', label="restore seeds snapshot render source before bootstrap")
    require(conversations_js, 'state.appendStream.attachMode = Number(state.appendStream.lastAppendId || 0) > 0 ? "sse-resume" : "awaiting-bootstrap";', label="restore attach mode seed")
    require(conversations_js, 'state.appendStream.resumeMode = Number(state.appendStream.lastAppendId || 0) > 0 ? "resuming" : "bootstrap";', label="restore resume mode seed")
    require(conversations_js, 'state.appendStream.resumeCursor = Math.max(Number(payload.resume_from_append_id || resumeCursor || 0), 0);', label="resume cursor bootstrap state")
    require(conversations_js, "normalizeSessionPhase", label="selected-thread phase normalization helper")
    require(conversations_js, "appendEnvelope.session_phase", label="append envelope session phase payload")
    require(conversations_js, "payload.session_phase", label="bootstrap session phase payload")
    require(conversations_js, "normalizeAutonomySummary", label="autonomy summary normalization helper")
    require(conversations_js, "hydrateAutonomySummary", label="autonomy summary hydration helper")
    require(conversations_js, "bootstrapAutonomySummary", label="bootstrap autonomy summary helper")
    require(conversations_js, "isSelectedThreadAutonomyAuthoritative", label="selected-thread autonomy authority helper")
    require(conversations_js, "shouldAllowGoalsPollingFallback", label="selected-thread goals fallback gate")
    require(conversations_js, "payload.autonomy_summary || payload.conversation?.autonomy_summary", label="bootstrap autonomy summary hydration")
    require(conversations_js, "conversation.autonomy_summary", label="snapshot autonomy summary hydration")
    require(conversations_js, 'freshnessState: bootstrapAutonomy ? "fresh" : "stale-or-missing"', label="bootstrap autonomy freshness authority")
    require(conversations_js, "fallbackAllowed: !bootstrapAutonomy", label="bootstrap autonomy fallback suppression")
    require(conversations_js, 'source: authoritativeBootstrapAutonomy ? "session-bootstrap" : "conversation-snapshot"', label="fetch conversation autonomy source precedence")
    require(conversations_js, 'freshnessState: authoritativeBootstrapAutonomy ? "fresh" : "stale-or-missing"', label="fetch conversation autonomy freshness precedence")
    require(conversations_js, "fallbackAllowed: !authoritativeBootstrapAutonomy", label="fetch conversation autonomy fallback precedence")
    require(conversations_js, "scheduleAppendStreamResume", label="resume scheduler helper")
    require(conversations_js, "transitionAppendStreamToFallback", label="explicit append stream fallback helper")
    require(conversations_js, "MAX_RESUME_ATTEMPTS = 3", label="bounded resume attempts")
    require(conversations_js, 'delivery_source: "sse"', label="SSE provenance tagging")
    require(conversations_js, "state.appendStream.transport = \"sse\"", label="SSE transport evidence")
    require(conversations_js, "syncConversationCardState", label="conversation card state sync helper")
    require(conversations_js, "showPendingOutgoing", label="pending outgoing render helper")
    require(conversations_js, "clearPendingOutgoing", label="pending outgoing clear helper")
    require(conversations_js, "showPendingAssistant", label="pending assistant handoff helper")
    require(conversations_js, "shouldClearPendingOutgoing", label="pending handoff clear predicate")
    require(conversations_js, "data-conversation-live-state", label="conversation card live state marker")
    require(conversations_js, "conversationPreview", label="conversation preview helper")
    require(conversations_js, "truncatePreview", label="bounded conversation preview helper")
    require(conversations_js, "latestMessagePreview", label="preferred message preview helper")
    require(conversations_js, "eventPreview", label="event preview helper")
    require(conversations_js, "snapshotThreadState", label="conversation snapshot state helper")
    require(conversations_js, "snapshotThreadLabel", label="conversation snapshot label helper")
    require(conversations_js, "compactConversationLabel", label="conversation live label helper")
    require_absent(conversations_js, "renderRecentThreadRail", label="removed recent-thread rail render helper")
    require_absent(conversations_js, "RECENT_THREAD_LIMIT = 4", label="removed recent-thread rail limit")
    require_absent(conversations_js, "data-recent-thread-chip", label="removed recent-thread chip DOM")
    require_absent(conversations_js, "data-recent-thread-marker", label="removed recent-thread marker DOM")
    require_absent(conversations_js, "data-recent-thread-state", label="removed recent-thread state DOM")
    require(conversations_js, "syncActiveSessionRow", label="sticky active session row helper")
    require(conversations_js, "threadTitleForConversation", label="thread switch title helper")
    require(conversations_js, "startThreadTransition", label="thread transition start helper")
    require(conversations_js, "clearThreadTransition", label="thread transition clear helper")
    require(conversations_js, 'state.currentConversationId = "";', label="thread switch clears current conversation before attach")
    require(conversations_js, "data-conversation-session", label="conversation session marker DOM")
    require(conversations_js, "data-conversation-live-owner-row", label="conversation live owner row DOM")
    require(conversations_js, "data-conversation-live-detail", label="conversation live owner detail DOM")
    require(conversations_js, "data-conversation-live-follow", label="conversation live owner follow DOM")
    require(conversations_js, "data-snapshot-thread-state", label="conversation snapshot state DOM")
    require(conversations_js, "data-snapshot-state-label", label="conversation snapshot state label DOM")
    require(conversations_js, 'data-preview-lines="1"', label="single-line conversation preview DOM")
    require(conversations_js, "dom.threadScroller?.dataset.sessionPresentation", label="left rail uses session presentation state")
    require(conversations_js, 'const selectedThreadSseOwned = selectedConversationId && selectedConversationId === liveConversationId && renderSource === "sse";', label="selected row sse ownership guard")
    require(conversations_js, 'marker.textContent = "NOW";', label="selected live owner marker label")
    require(conversations_js, 'card.dataset.liveOwnerState = "idle";', label="selected live owner state dataset")
    require(conversations_js, 'card.dataset.liveOwnerState = "idle";', label="selected live follow state dataset")
    require(conversations_js, "sessionMarker.hidden = !isSelected", label="selected thread session marker visibility")
    require(conversations_js, 'card.dataset.liveOwner = "false";', label="selected live owner dataset")
    require(conversations_js, 'card.dataset.liveOwnerShadow = "false";', label="selected live owner shadow dataset reset")
    require(conversations_js, 'const activeRowModel = deriveSelectedThreadActiveSessionRowModel(state, state.conversationCache);', label="active row model wiring")
    require(conversations_js, 'const selectedRowModel = deriveSelectedThreadConversationRowLiveModel(state, state.conversationCache);', label="selected row live model wiring")
    require(conversations_js, 'liveOwnerRow.hidden = !showSelectedRowLiveMarker;', label="selected row live owner row visibility")
    require(conversations_js, 'const shadowSelectedRowLiveMarker =', label="selected row live owner shadow guard")
    require(conversations_js, 'selectedRowModel.rowOwned &&', label="selected row sse-owned visibility gate")
    require(conversations_js, 'selectedRowModel.rowSource === "sse" &&', label="selected row sse source visibility gate")
    require(conversations_js, 'card.dataset.liveOwnerShadow = String(shadowSelectedRowLiveMarker);', label="selected row card shadow dataset")
    require(conversations_js, 'liveOwnerRow.dataset.liveOwnerShadow = showSelectedRowLiveMarker ? String(shadowSelectedRowLiveMarker) : "false";', label="selected row live owner shadow dataset")
    require(conversations_js, 'liveOwnerDetail.textContent = showSelectedRowLiveMarker ? selectedRowModel.markerLabel : "LIVE";', label="selected row live owner detail label")
    require(conversations_js, 'liveOwnerFollow.dataset.liveOwnerCue = showSelectedRowLiveMarker ? selectedRowModel.cueKind : "idle";', label="selected row live owner cue dataset")
    require(conversations_js, "liveState.hidden = isSelected", label="selected thread snapshot badge hiding")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionState = visible ? rowState : "idle";', label="active session row state dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionConversationId = visible ? conversationId : "";', label="active session row conversation dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionFollow = visible ? followLabel.toLowerCase() : "idle";', label="active session row follow dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionOwned = visible ? String(rowOwned) : "false";', label="active session row ownership dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionCanonical = visible ? String(Boolean(canonical)) : "false";', label="active session row canonical dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionSource = visible ? rowSource : "none";', label="active session row source dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionPhase = visible ? rowPhase : "IDLE";', label="active session row phase dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionUnseenCount = String(visible ? rowUnseenCount : 0);', label="active session row unseen dataset")
    require(conversations_js, 'dom.activeSessionRow.dataset.activeSessionClearReason = visible ? clearReason : "idle";', label="active session row clear reason dataset")
    require(conversations_js, "const authoritativeSelectedAttach =", label="selected-thread attach authority gate")
    require(conversations_js, "if (authoritativeSelectedAttach) {", label="selected-thread attach authority short circuit")
    require(store_js, "export function deriveSelectedThreadActiveSessionRowModel", label="active session row store helper")
    require(store_js, "export function deriveSelectedThreadConversationRowLiveModel", label="selected row live model store helper")
    require(store_js, "canonical: true,", label="active session canonical owned state")
    require(store_js, "canonical: false,", label="active session non-canonical state")
    require(conversations_js, 'const rowModel = deriveSelectedThreadActiveSessionRowModel(state, state.conversationCache);', label="active session row store model wiring")
    require(store_js, 'markerLabel =', label="selected row marker label mapping")
    require(store_js, 'cueLabel =', label="selected row cue label mapping")
    require(store_js, 'cueKind =', label="selected row cue kind mapping")
    require(store_js, 'presentation: "switching",', label="active session switching presentation")
    require(store_js, 'rowState: "switching",', label="active session switching state")
    require(store_js, 'rowSource: "thread-transition",', label="active session switching source")
    require(store_js, 'rowPhase: "SWITCHING",', label="active session switching phase")
    require(store_js, 'followLabel: "ATTACH",', label="active session switching follow label")
    require(store_js, 'meta: "selected thread · switching · attach pending",', label="active session switching meta copy")
    require(store_js, 'presentation: "handoff",', label="active session handoff presentation")
    require(store_js, 'rowState: "handoff",', label="active session handoff state")
    require(store_js, 'stateLabel: "HANDOFF",', label="active session handoff label")
    require(store_js, 'const rowState = followControl.visible ? followControl.followState : "live";', label="active session owned state mapping")
    require(store_js, 'const followLabel = followControl.visible ? followControl.stateLabel : "LIVE";', label="active session owned follow mapping")
    require(store_js, 'const rowPhase = phaseLabel || "LIVE";', label="active session owned phase mapping")
    require(store_js, '`selected thread · ${rowPhase.toLowerCase()} · ${rowUnseenCount} new`', label="active session unseen meta copy")
    require(store_js, '`selected thread · ${rowPhase.toLowerCase()} · ${followControl.detailLabel}`', label="active session paused meta copy")
    require(store_js, '`selected thread · ${rowPhase.toLowerCase()} · sse owner`', label="active session healthy meta copy")
    require(conversations_js, "syncSelectedSessionFromLiveAppend", label="selected-thread live append sync helper")
    require(conversations_js, "const liveJobId = String(livePayload.job_id || \"\").trim();", label="live append job id extraction")
    require(conversations_js, "state.currentJobId = liveJobId;", label="live append selected-thread job id authority")
    require(conversations_js, "conversation.latest_job_id = liveJobId;", label="live append conversation job id authority")
    require(conversations_js, "shouldProjectAutonomySummaryFromLiveAppend", label="autonomy summary append projection helper")
    require(conversations_js, "projectAutonomySummaryFromLiveAppend", label="autonomy summary live projection helper")
    require(conversations_js, "autonomySummaryFromSessionStatus", label="session-status autonomy summary helper")
    require(conversations_js, "isSelectedThreadSessionStatusAutonomyAuthoritative", label="session-status autonomy authority helper")
    require(conversations_js, "liveJobMetaLabel", label="live append job meta helper")
    require(conversations_js, "const sessionStatusAutonomySummary = autonomySummaryFromSessionStatus(", label="append session-status autonomy summary hydration")
    require(conversations_js, "const projectedAutonomySummary = projectAutonomySummaryFromLiveAppend(kind, payload);", label="append-driven autonomy summary projection")
    require(conversations_js, "state.autonomySummary = projectedAutonomySummary;", label="append-driven autonomy summary state update")
    require(conversations_js, 'source: "sse",', label="append-driven autonomy summary sse source")
    require(conversations_js, 'freshnessState: "fresh",', label="append-driven autonomy summary fresh state")
    require(conversations_js, "fallbackAllowed: false,", label="append-driven autonomy summary fallback suppression metadata")
    require(conversations_js, 'source: authoritative ? "session-status" : `session-status-${transportState || "degraded"}`', label="session-status autonomy source selection")
    require(conversations_js, 'String(conversationId) === String(state.currentConversationId || "") ||', label="selected-thread goals fallback current conversation suppression")
    require(conversations_js, 'String(conversationId) === String(state.savedConversationId || "")', label="selected-thread goals fallback saved conversation suppression")
    require_absent(conversations_js, 'await refreshGoalSummary({ conversationId: conversation.conversation_id });', label="removed selected-thread goals polling refresh")
    require(conversations_js, "const selectedThreadAutonomy =", label="bootstrap session-status autonomy selection")
    require(conversations_js, "state.autonomySummary = selectedThreadAutonomy;", label="bootstrap session-status autonomy state update")
    require(conversations_js, 'state.appendStream?.attachMode === "sse-bootstrap" ||', label="bootstrap attach healthy-path polling suppression")
    require(conversations_js, "state.appendStream?.sessionStatus?.proposalJobId ||", label="bootstrap session-status proposal job id authority")
    require_absent(conversations_js, "refreshGoalSummary().catch(() => {});", label="legacy append-driven autonomy summary refetch")
    require(conversations_js, "setJobMeta(dom, immediateMeta);", label="append-driven job meta refresh")
    require(conversations_js, 'type === "job.running"', label="job running append refresh mapping")
    require(conversations_js, 'type === "proposal.ready"', label="proposal ready append refresh mapping")
    require(conversations_js, 'type.startsWith("goal.review.phase.")', label="review append refresh mapping")
    require(conversations_js, 'type.startsWith("goal.verify.phase.")', label="verify append refresh mapping")
    require(conversations_js, "scheduleAppendStreamResume(conversationId, Math.max(reconnectAttempt, 0) + 1);", label="reconnect resume scheduling")
    require_absent(
        conversations_js,
        'state.appendStream.status = "reconnecting";\n      state.appendStream.attachMode = "snapshot-fallback";\n      ensurePollingForJob();',
        label="legacy reconnect snapshot fallback",
    )
    require(store_js, "export function isAppendStreamAuthoritative", label="append stream authoritative helper")
    require(store_js, "export function deriveSelectedThreadSessionStatus", label="canonical selected-thread session status helper")
    require(store_js, "selectedThreadProvisional", label="selected-thread provisional state")
    require(store_js, "selectedThreadRestore", label="selected-thread restore state")
    require(store_js, "provisionalResume", label="selected-thread provisional resume state")
    require(store_js, "restoreResume", label="selected-thread restore resume state")
    require(store_js, "export function deriveSelectedThreadHealthyPromotionModel", label="selected-thread healthy promotion helper")
    require(store_js, "const selectedThreadAuthoritative =", label="selected-thread healthy promotion authority guard")
    require(store_js, 'const payloadHealthy = sameConversation && payloadTransportState === "sse-live";', label="selected-thread healthy promotion payload guard")
    require(store_js, "const promoted = selectedThreadAuthoritative && payloadHealthy;", label="selected-thread healthy promotion invariant")
    require(store_js, "restoreStage", label="selected-thread restore stage")
    require(store_js, "switchActive", label="selected-thread switch activity state")
    require(store_js, "switchConversationId", label="selected-thread switch conversation state")
    require(store_js, "switchTargetTitle", label="selected-thread switch title state")
    require(store_js, 'transportReason = provisionalResume ? "selected-thread-resume" : "selected-thread-attach";', label="selected-thread provisional transport reason")
    require(store_js, 'transportReason = restoreResume ? "saved-restore-resume" : "saved-restore-attach";', label="selected-thread restore transport reason")
    require(store_js, 'presentation = "provisional";', label="selected-thread provisional presentation")
    require(store_js, 'presentation = "restore";', label="selected-thread restore presentation")
    require(store_js, "export function deriveSelectedThreadLiveAutonomy", label="canonical selected-thread live autonomy helper")
    require(store_js, "const healthyPromotion = deriveSelectedThreadHealthyPromotionModel(currentState, conversation);", label="live autonomy healthy promotion helper wiring")
    require(store_js, "const sessionStatusPayload = currentState.appendStream?.sessionStatus || null;", label="store session-status autonomy payload read")
    require(store_js, 'source: "session-status",', label="store session-status autonomy source")
    require(store_js, "const canonicalAutonomySummary = sessionStatusSummary || autonomySummary;", label="store session-status autonomy precedence")
    require(store_js, "export function deriveSelectedThreadPhaseProgression", label="canonical selected-thread phase progression helper")
    require(store_js, "export function deriveSelectedThreadShellPhaseLabel", label="canonical selected-thread shell phase helper")
    require(store_js, "export function deriveSelectedThreadTimelineMilestones", label="canonical selected-thread timeline milestones helper")
    require(store_js, '{ key: "auto-apply", label: "AUTO APPLY", state: phaseRank > 3 ? "complete" : currentPhase === "AUTO APPLY" ? "active" : "pending" },', label="explicit auto-apply milestone item")
    require(store_js, "export function deriveSelectedThreadSessionSurfaceModel", label="canonical selected-thread session surface helper")
    require(store_js, "export function deriveSelectedThreadSessionAuthorityModel", label="canonical selected-thread authority model helper")
    require(store_js, "const sessionStatus = deriveSelectedThreadSessionStatus(currentState, conversation);", label="session surface canonical status helper read")
    require(store_js, "const healthyPromotion = deriveSelectedThreadHealthyPromotionModel(currentState, conversation);", label="session surface healthy promotion helper wiring")
    require(store_js, "const liveAutonomy = deriveSelectedThreadLiveAutonomy(currentState, conversation);", label="session surface canonical live autonomy helper read")
    require(store_js, "const phaseProgression = deriveSelectedThreadPhaseProgression(currentState, conversation);", label="session surface canonical phase progression helper read")
    require(store_js, "const milestoneModel = deriveSelectedThreadTimelineMilestones(currentState, conversation);", label="session surface canonical milestones helper read")
    require(store_js, "const shellPhaseLabel = deriveSelectedThreadShellPhaseLabel(currentState, conversation);", label="session surface canonical shell phase helper read")
    require(store_js, "healthyPromotion.promoted && sessionSurface.liveOwned && sessionStrip.visible && sessionStrip.owned", label="authority healthy promotion gate")
    require(store_js, 'pathVerdict: liveOwned ? String(summary?.pathVerdict || "UNKNOWN").toUpperCase() : "",', label="session surface path verdict projection")
    require(store_js, 'verifierAcceptability: liveOwned ? String(summary?.verifierAcceptability || "PENDING").toUpperCase() : "",', label="session surface verifier acceptability projection")
    require(store_js, 'blockerReason: liveOwned ? String(summary?.blockerReason || "none").toUpperCase() : "",', label="session surface blocker reason projection")
    require(store_js, "export function isSelectedThreadSessionOwned", label="selected-thread session ownership helper")
    require(store_js, "healthyPromotion.promoted &&", label="selected-thread session ownership promotion gate")
    require(store_js, 'phaseValue === "LIVE"', label="selected-thread session live phase guard")
    require(store_js, 'phaseValue === "PROPOSAL"', label="selected-thread session proposal phase guard")
    require(store_js, 'phaseValue === "REVIEW"', label="selected-thread session review phase guard")
    require(store_js, 'phaseValue === "VERIFY"', label="selected-thread session verify phase guard")
    require(store_js, 'phaseValue === "READY"', label="selected-thread session ready phase guard")
    require(store_js, 'phaseValue === "APPLIED"', label="selected-thread session applied phase guard")
    require(store_js, 'appendStream.status === "connecting" || appendStream.status === "live"', label="append stream authoritative connecting-or-live guard")
    require(jobs_js, "isAppendStreamAuthoritative", label="job polling authoritative helper wiring")
    require(jobs_js, "isSelectedThreadSessionOwned", label="job polling selected-thread session ownership helper wiring")
    require(jobs_js, "selectedThreadPollingFallbackAllowed", label="job polling selected-thread fallback gate")
    require(jobs_js, "!selectedThreadPollingFallbackAllowed()", label="polling suppression while selected-thread session is sse-owned")
    require(jobs_js, "stopPolling();\n      return;", label="polling early exit while selected-thread session is sse-owned")
    require(jobs_js, 'if (!selectedThreadPollingFallbackAllowed()) {\n      return payload;\n    }', label="late poll visible-state suppression")
    require(jobs_js, "selectedThreadPollingFallbackAllowed(state.currentConversationId)", label="poll-driven conversation refresh fallback gate")
    require_absent(jobs_js, 'await refreshGoalSummary();', label="removed poll-driven goals refresh fallback")
    require(conversations_js, 'return "CONNECT";', label="compact connecting rail label")
    require(conversations_js, 'return "RESUME";', label="compact reconnect rail label")
    require(conversations_js, 'return "FAILED";', label="compact failed rail label")
    require(conversations_js, 'return "READY";', label="compact ready rail label")
    require(conversations_js, 'return "VERIFY";', label="compact verify rail label")
    require(conversations_js, 'return "REVIEW";', label="compact review rail label")
    require(conversations_js, 'return "PROPOSAL";', label="compact proposal rail label")
    require(conversations_js, 'return "WAITING";', label="compact waiting rail label")
    require(conversations_js, 'return "ACTIVE";', label="compact active rail label")
    require(conversations_js, 'return "DONE";', label="compact done rail label")
    require(conversations_js, 'return "IDLE";', label="compact idle rail label")
    require(conversations_js, "shellPhaseLabel: deriveSelectedThreadShellPhaseLabel(state, state.conversationCache)", label="selected-card shell phase helper read")
    require(conversations_js, "메시지가 접수되어 첫 응답을 기다리는 중입니다.", label="accepted event preview copy")
    require(conversations_js, "작업이 대기열에 올라 있습니다.", label="queued event preview copy")
    require(conversations_js, "최근 작업이 현재 진행 중입니다.", label="running event preview copy")
    require(conversations_js, "제안 단계가 진행 중입니다.", label="proposal event preview copy")
    require(conversations_js, "리뷰 단계가 진행 중입니다.", label="review event preview copy")
    require(conversations_js, "검증 단계가 진행 중입니다.", label="verify event preview copy")
    require(conversations_js, "자동 적용 단계가 진행 중입니다.", label="auto-apply event preview copy")
    require(jobs_js, "!isAppendStreamAuthoritative(state, state.currentConversationId)", label="polling refetch skip while append stream is authoritative")
    require(render_js, "renderSessionSummary", label="header session summary helper")
    require(render_js, "composerOwnerState", label="composer strip owner state helper")
    require(store_js, "export function deriveSelectedThreadComposerTargetRowModel(currentState, conversation = null) {", label="composer target row model helper")
    require(render_js, "composerTransportState", label="composer transport state helper")
    require(render_js, "syncComposerOwnership", label="composer ownership sync helper")
    require(render_js, "compactTargetLabel", label="compact target label helper")
    require(render_js, "summaryHint", label="summary hint helper")
    require(render_js, "joinSessionChromeTokens", label="session chrome token join helper")
    require(render_js, "sessionFollowLabel", label="session follow token helper")
    require(render_js, "proposalStatusLabel", label="session proposal token helper")
    require(render_js, "sessionChromeCopy", label="session summary chrome copy helper")
    require(render_js, "sessionStripDetailCopy", label="session strip detail copy helper")
    require(render_js, "sessionStripStateChipMarkup", label="session strip chip markup helper")
    require(render_js, "sessionStripStateRow", label="session strip single-row state helper")
    require(render_js, "selectedThreadFooterDockModel", label="footer session dock helper")
    require(render_js, "const sessionSurface = deriveSelectedThreadSessionSurfaceModel(currentState, conversation);", label="shared selected-thread session surface wiring")
    require(render_js, 'const liveOwned = authority.state === "healthy";', label="footer dock shared live ownership wiring")
    require(render_js, "const phaseLabel = String(", label="footer dock shared phase label wiring")
    require(render_js, 'source: String(authority.source || sessionSurface.milestoneModel.source || sessionSurface.source || "sse").toLowerCase(),', label="footer dock shared source wiring")
    require(render_js, 'label: "SWITCHING"', label="composer switching label")
    require(render_js, 'label: "HANDOFF"', label="composer handoff label")
    require(render_js, 'label: "READY"', label="composer ready label")
    require(render_js, 'copy: "ATTACH"', label="composer switching compact copy")
    require(render_js, 'pendingOutgoing.status === "sending-user"', label="composer sending compact copy guard")
    require(render_js, '? "SEND"', label="composer sending compact copy")
    require(render_js, ': "FIRST"', label="composer accepted compact copy")
    require(render_js, 'copy: "SSE OWNER"', label="composer ready compact copy")
    require(render_js, 'copy: "SELECT"', label="composer idle compact copy")
    require(render_js, "const owner = deriveSelectedThreadComposerTargetRowModel(currentState, conversation);", label="composer target row canonical authority wiring")
    require(render_js, 'dom.sendRequestButton.dataset.composerBlocked = owner.blocked ? "true" : "false";', label="composer blocked dataset")
    require(render_js, 'dom.sendRequestButton.dataset.composerOwnerState = owner.state;', label="composer owner state dataset")
    require(render_js, 'dom.sendRequestButton.dataset.composerBlockedReason = owner.blockedReason;', label="composer blocked reason dataset")
    require(render_js, 'const mergedIntoStrip = Boolean(dom.sessionStrip && !dom.sessionStrip.hidden && owner.conversationId);', label="composer owner merged strip guard")
    require(render_js, 'dom.composerOwnerRow.dataset.composerOwnerMerged = mergedIntoStrip ? "true" : "false";', label="composer owner merged dataset")
    require(render_js, 'dom.composerOwnerRow.hidden = mergedIntoStrip || owner.state === "idle";', label="composer owner row hidden when merged")
    require(render_js, 'dom.sessionStrip.dataset.composerState = ownerState.state;', label="composer strip state dataset")
    require(render_js, 'dom.sessionStrip.dataset.composerTransport = transportState.key;', label="composer strip transport dataset")
    require(render_js, 'dom.sessionStrip.dataset.composerTransportSource = transportState.source;', label="composer strip transport source dataset")
    require(render_js, 'dom.sessionStrip.dataset.composerTransportOwned = stripLiveOwned ? "true" : "false";', label="composer strip transport owned dataset")
    require(render_js, 'dom.sessionStrip.dataset.composerTransportReason = transportState.reason;', label="composer strip transport reason dataset")
    require(render_js, 'dom.sessionStrip.dataset.composerTargetConversationId = ownerState.conversationId;', label="composer strip target dataset")
    require(render_js, "const proposalState = proposalChip(liveRun);", label="composer strip proposal chip helper wiring")
    require(render_js, 'label: "ATTACH"', label="composer strip attach label")
    require(render_js, 'label: "SNAPSHOT"', label="composer strip snapshot label")
    require(store_js, 'transportLabel = "POLLING";', label="composer strip polling label")
    require(store_js, 'transportLabel = "RECONNECT";', label="composer strip reconnect label")
    require(render_js, 'label: "SWITCHING",', label="composer strip switching row")
    require(render_js, 'label: "TARGET",', label="composer strip context row")
    require(render_js, "chips: footerDock.chips,", label="composer strip footer-dock chip row")
    require(render_js, 'label: transportState.label,', label="composer strip degraded row")
    require(render_js, "selectedThreadLiveSessionIndicator", label="header live-session indicator helper")
    require(render_js, "latestSessionIndicatorEvent", label="header live-session event helper")
    require(render_js, "const switchingSelectedThread = Boolean(threadTransition.active && threadTransition.targetConversationId);", label="header switching guard")
    require(render_js, 'const badgeVisible =', label="header single-badge visibility helper")
    require(render_js, 'const badgeStateLabel =', label="header single-badge path-state helper")
    require(render_js, 'const badgeTone =', label="header single-badge tone helper")
    require(render_js, 'const badgeSource =', label="header single-badge source helper")
    require(render_js, 'const badgePresentation =', label="header single-badge presentation helper")
    require(render_js, 'const badgeReason =', label="header single-badge reason helper")
    require(render_js, 'const badgeDetail =', label="header single-badge detail helper")
    require(render_js, 'const healthyPhaseLabel = String(', label="header healthy phase label helper")
    require(render_js, "const healthyTranscriptAuthority =", label="header healthy timeline authority helper")
    require(render_js, "const provisionalTranscriptAuthority =", label="header provisional timeline authority helper")
    require(render_js, "const summaryVisible = authority.summaryVisible && !healthyTranscriptAuthority && !provisionalTranscriptAuthority;", label="header healthy summary suppression")
    require(render_js, "dom.threadSessionSummaryOwner", label="header session summary owner chip wiring")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionVisible = summaryVisible ? "true" : "false";', label="header summary visible dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionPresentation = summaryVisible ? badgePresentation : "cleared";', label="header summary presentation dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionSource = summaryVisible ? badgeSource : "none";', label="header summary source dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionOwned = summaryVisible && authority.owned ? "true" : "false";', label="header summary ownership dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionReason = summaryVisible ? badgeReason : "idle";', label="header summary reason dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionStateLabel = summaryVisible ? badgeStateLabel : "";', label="header summary state label dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionPhase = summaryVisible ? healthyPhaseLabel : "";', label="header summary live phase dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionDetail = summaryVisible ? badgeDetail : "현재 활성 세션이 없습니다.";', label="header summary detail dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.liveSessionProvenance = summaryVisible ? badgeSource : "none";', label="header summary provenance dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="header summary authority dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="header summary presentation authority dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.restoreStage = sessionStatus.restoreStage || "none";', label="header summary restore stage dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.restorePath = sessionStatus.restorePath || "none";', label="header summary restore path dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.restoreProvenance = sessionStatus.restoreProvenance || "none";', label="header summary restore provenance dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.threadConversationId = conversationId;', label="header summary conversation dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.threadSummaryTone = summaryVisible ? badgeTone : "muted";', label="header summary tone dataset")
    require(render_js, 'dom.threadSessionSummary.title = summaryVisible ? badgeDetail : "현재 활성 세션이 없습니다.";', label="header summary title copy")
    require(store_js, 'transportLabel = "RECONNECT";', label="header live-session reconnect label")
    require(store_js, 'transportLabel = "POLLING";', label="header live-session polling label")
    require(render_js, "phaseDetailCopy", label="phase detail copy helper")
    require(render_js, "compactPhaseDetailCopy", label="compact phase detail copy helper")
    require(render_js, "function sessionCompactTarget(", label="compact selected-thread target helper")
    require(render_js, 'dom.threadPhaseChip.hidden = true;', label="legacy header phase badge forced hidden")
    require(render_js, 'const followPhaseLabel = footerDock?.phaseLabel || ownerState.label || "READY";', label="footer follow phase label helper")
    require(store_js, 'transportReason = followPaused ? "selected-thread-follow-paused" : "selected-thread-following";', label="header live-follow reason")
    require(store_js, 'transportReason = sessionRotationDetected ? "session-rotation" : retrying ? "retrying" : "polling-fallback";', label="header live-session degraded reason")
    require(store_js, 'const retrying = latestType === "codex.exec.retrying";', label="header live-session retry event mapping")
    require(render_js, "return target;", label="summary or composer compact target copy")
    require(render_js, "deriveLiveRunState", label="inline live run state derivation")
    require(render_js, "selectedThreadSessionPhase", label="selected-thread authoritative phase helper")
    require(render_js, 'state: "sending"', label="local pending sending state")
    require(render_js, 'state: "generating"', label="assistant placeholder generating state")
    require(render_js, 'state: "proposal-phase"', label="proposal phase live run state")
    require(render_js, 'state: "review-phase"', label="review phase live run state")
    require(render_js, 'state: "verify-phase"', label="verify phase live run state")
    require(render_js, 'state: "auto-apply"', label="auto-apply live run state")
    require(render_js, 'state: "proposal-ready"', label="proposal ready live run state")
    require(render_js, 'state: "applied"', label="applied live run state")
    require(render_js, 'state: sessionPhase.value === "LIVE" ? "live" : "unknown"', label="neutral live run state")
    require(render_js, 'phase: "PROPOSAL"', label="proposal phase label")
    require(render_js, 'phase: "REVIEW"', label="review phase label")
    require(render_js, 'phase: "VERIFY"', label="verify phase label")
    require(render_js, 'phase: "AUTO APPLY"', label="auto-apply phase label")
    require(render_js, 'phase: "READY"', label="proposal ready phase label")
    require(render_js, 'phase: "APPLIED"', label="applied phase label")
    require(render_js, 'phase: sessionPhase.value === "LIVE" ? "LIVE" : "UNKNOWN"', label="neutral live phase label")
    require(render_js, 'goal.proposal.phase.started', label="proposal phase event mapping")
    require(render_js, 'goal.review.phase.started', label="review phase event mapping")
    require(render_js, 'goal.verify.phase.started', label="verify phase event mapping")
    require(render_js, "renderSessionStrip", label="unified session strip renderer")
    require(render_js, "renderThreadTransition", label="thread transition render helper")
    require(render_js, "function renderThreadTransition(currentState, sessionStatus = deriveSelectedThreadSessionStatus(currentState, null))", label="thread transition canonical status signature")
    require(render_js, 'data-thread-transition="switching"', label="thread transition DOM")
    require(render_js, 'data-thread-transition-source="selected-thread-session"', label="thread transition source dataset")
    require(render_js, 'data-thread-transition-owner-cleared="true"', label="thread transition ownership-cleared dataset")
    require(render_js, 'data-thread-transition-live-strip-cleared="true"', label="thread transition live-strip-cleared dataset")
    require(render_js, 'data-thread-transition-compact="true"', label="thread transition compact dataset")
    require(render_js, 'class="timeline-transition-target"', label="thread transition compact target label")
    require(render_js, 'data-thread-transition-phase-label="${escapeHtml(phaseLabel)}"', label="thread transition phase label dataset")
    require(render_js, 'data-thread-transition-state-label="${escapeHtml(stateLabel)}"', label="thread transition state label dataset")
    require(render_js, 'data-thread-transition-transport="${escapeHtml(transportLabel)}"', label="thread transition transport dataset")
    require(render_js, 'data-thread-transition-restore-stage="${escapeHtml(String(sessionStatus.restoreStage || "none"))}"', label="thread transition restore stage dataset")
    require(render_js, 'data-thread-transition-restore-path="${escapeHtml(String(sessionStatus.restorePath || "none"))}"', label="thread transition restore path dataset")
    require(render_js, 'data-thread-transition-restore-provenance="${escapeHtml(String(sessionStatus.restoreProvenance || "none"))}"', label="thread transition restore provenance dataset")
    require(render_js, "selectedThreadWorkspacePlaceholder", label="thread transition workspace placeholder helper")
    require(render_js, "const workspacePlaceholder = selectedThreadWorkspacePlaceholder(currentState);", label="thread transition workspace placeholder state")
    require(render_js, "if (isSwitching) {", label="switch placeholder branch")
    require(render_js, "if (isRestore) {", label="restore placeholder branch")
    require(render_js, 'workspaceSummary: "selected thread switching · target snapshot attach pending",', label="switch workspace summary copy")
    require(render_js, 'workspaceSummary: sessionStatus.restoreResume', label="restore workspace summary copy")
    require(render_js, 'workspaceSummary: "선택된 대화가 없으면 현재 세션 맥락이 여기에 정리됩니다.",', label="empty workspace summary copy")
    require(render_js, 'dom.conversationTimeline.dataset.workspacePlaceholder = "conversation";', label="workspace placeholder mode dataset")
    require(render_js, 'dom.conversationTimeline.dataset.workspaceConversationId = workspacePlaceholder.conversationId;', label="workspace placeholder conversation dataset")
    require(render_js, 'dom.conversationTimeline.dataset.workspaceOwnerCleared = isThreadTransition || isSavedRestore ? "true" : "false";', label="workspace placeholder ownership-cleared dataset")
    require(render_js, 'dom.conversationTimeline.innerHTML = workspacePlaceholder.timeline;', label="thread transition placeholder render path")
    require(render_js, 'dom.threadScroller.dataset.workspacePlaceholder = "conversation";', label="thread scroller placeholder mode dataset")
    require(render_js, 'dom.threadScroller.dataset.workspacePlaceholderConversationId = workspacePlaceholder.conversationId;', label="thread scroller placeholder conversation dataset")
    require(render_js, 'dom.threadScroller.dataset.workspaceOwnerCleared = isThreadTransition || isSavedRestore ? "true" : "false";', label="thread scroller ownership-cleared dataset")
    require(render_js, 'data-thread-transition-conversation-id="${escapeHtml(String(sessionStatus.switchConversationId || sessionStatus.targetConversationId || ""))}"', label="thread transition canonical conversation dataset")
    require(render_js, 'mode: "switching"', label="switch workspace placeholder mode")
    require(render_js, 'mode: "restore"', label="restore workspace placeholder mode")
    require(render_js, 'mode: "empty"', label="empty workspace placeholder mode")
    require(render_js, "timeline: renderThreadTransition(currentState, sessionStatus),", label="switch workspace timeline")
    require(render_js, 'timeline: renderThreadTransition(currentState, sessionStatus),', label="restore workspace timeline")
    require(render_js, 'timeline: \'<p class="timeline-empty">새 대화를 만들면 요청과 이벤트가 여기 쌓입니다.</p>\',', label="true empty workspace timeline")
    require(render_js, 'threadTitle: workspacePlaceholder.title,', label="thread transition or restore title copy")
    require(render_js, 'conversationState: workspacePlaceholder.conversationState,', label="thread transition or restore conversation state copy")
    require(render_js, 'liveRun: workspacePlaceholder.liveRun,', label="thread transition or restore neutral phase")
    require(render_js, "renderWorkspaceSummary(dom, workspacePlaceholder.workspaceSummary);", label="workspace placeholder summary render path")
    require(render_js, 'dom.threadScroller.dataset.threadTransitionState = isThreadTransition ? "switching" : "idle";', label="thread transition state dataset")
    require(render_js, 'dom.threadScroller.dataset.threadTransitionConversationId = isThreadTransition', label="thread transition conversation dataset")
    require(render_js, 'dom.conversationTimeline.dataset.liveSessionStripVisible = "false";', label="workspace placeholder clears center strip visibility")
    require(render_js, 'dom.threadScroller.dataset.liveSessionStripClearReason = sessionStripModel.clearReason || (isThreadTransition ? "thread-switch" : "deselected");', label="workspace placeholder center strip clear reason dataset")
    require(render_js, 'dom.conversationTimeline.dataset.liveSessionStripVisible = sessionStripModel.visible ? "true" : "false";', label="conversation center strip visibility dataset")
    require(render_js, 'dom.threadScroller.dataset.liveSessionStripOwned = sessionStripModel.owned ? "true" : "false";', label="thread scroller center strip ownership dataset")
    require(render_js, 'renderSessionStrip(dom, currentState, null);', label="thread transition composer shell render path")
    require(render_js, 'syncComposerOwnership(dom, currentState, null);', label="thread transition composer owner switching path")
    require(render_js, 'dom.sendRequestButton.dataset.composerOwnerConversationId = owner.conversationId;', label="composer owner target dataset")
    require(render_js, 'dom.threadScroller.dataset.workspacePlaceholder = "conversation";', label="conversation scroller placeholder reset")
    require(render_js, 'dom.threadScroller.dataset.workspacePlaceholderConversationId = String(conversation.conversation_id || "");', label="conversation scroller placeholder conversation reset")
    require(render_js, 'dom.conversationTimeline.dataset.workspaceOwnerCleared = "false";', label="conversation workspace ownership-cleared reset")
    require(render_js, 'dom.threadScroller.dataset.workspaceOwnerCleared = "false";', label="conversation scroller ownership-cleared reset")
    require(render_js, "renderRestoreSessionTimeline", label="restore transcript timeline helper")
    require(render_js, 'conversationId: isThreadTransition', label="thread transition or restore live-follow target reset")
    require(render_js, 'presentation === "sending"', label="sending presentation state")
    require(render_js, 'data-pending-local="true"', label="pending local timeline marker")
    require(render_js, 'data-pending-assistant="true"', label="pending assistant timeline marker")
    require(render_js, "local-pending", label="pending local render source")
    require(render_js, "local-assistant-placeholder", label="assistant placeholder render source")
    require(render_js, "pendingHandoffState", label="single pending handoff render helper")
    require(render_js, 'stage: "pending-user"', label="pending user handoff stage")
    require(render_js, 'stage: "pending-assistant"', label="pending assistant handoff stage")
    require(render_js, "selectedThreadSseOwned", label="pending assistant selected-thread SSE ownership guard")
    require(render_js, "pendingOutgoing.status === \"awaiting-assistant\" &&", label="awaiting-assistant ownership render guard")
    require(render_js, 'const sessionStatus = deriveSelectedThreadSessionStatus(currentState, conversation);', label="inline canonical session status helper")
    require(render_js, 'const handoffVisible = handoffState.stage === "pending-assistant" && sessionStatus.selectedThreadSse;', label="inline handoff visibility guard")
    require(render_js, "const liveVisible =", label="inline live visibility guard")
    require(render_js, "sessionStatus.liveOwned &&", label="inline live healthy ownership guard")
    require(render_js, "pendingUserCount", label="pending user count dataset state")
    require(render_js, "pendingAssistantCount", label="pending assistant count dataset state")
    require(render_js, "handoffState.items", label="single pending handoff item render wiring")
    require(render_js, 'dom.threadScroller.dataset.pendingHandoffStage = handoffState.stage', label="pending handoff stage dataset")
    require(render_js, 'dom.threadScroller.dataset.pendingUserCount = String(handoffState.pendingUserCount)', label="pending user count dataset")
    require(render_js, 'dom.threadScroller.dataset.pendingAssistantCount = String(handoffState.pendingAssistantCount)', label="pending assistant count dataset")
    require(conversations_js, "isCurrentThreadTransition", label="thread transition freshness helper")
    require(conversations_js, "threadTransitionRequestId", label="thread transition request id state")
    require(conversations_js, "requestId: nextRequestId", label="thread transition request id payload")
    require(conversations_js, "const replacingTransition =", label="replacement thread transition guard")
    require(conversations_js, "transitionRequestId = startThreadTransition(conversationId);", label="thread transition request capture")
    require(conversations_js, "if (transitionRequestId && !isCurrentThreadTransition(conversationId, transitionRequestId)) {", label="stale thread transition guard")
    require(conversations_js, "clearThreadTransition({ conversationId, requestId: transitionRequestId });", label="scoped thread transition clear")
    require(conversations_js, "syncSelectedAppSession", label="selected app session sync helper")
    require(conversations_js, 'option.dataset.sessionId = app.session_id || "";', label="selected app session id option dataset")
    require(conversations_js, "rotationDetected", label="selected app session rotation detection")
    require(jobs_js, "renderJobActivity(dom, state.conversationCache, state.currentJobId, payload, state);", label="job panel receives current state for SSE ownership")
    require(render_js, "autonomyDetailMeta", label="secondary panel autonomy detail meta wiring")
    require(render_js, "renderSecondaryPanelSessionFacts", label="secondary panel session facts helper")
    require(render_js, "secondaryPanelSessionFactsModel", label="secondary panel session facts model")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsPresentation = facts.presentation;', label="secondary panel facts presentation dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsOwned = facts.owned ? "true" : "false";', label="secondary panel facts ownership dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsTransport = facts.transport;', label="secondary panel facts transport dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsPhase = facts.phase;', label="secondary panel facts phase dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsPath = facts.path;', label="secondary panel facts path dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsVerifier = facts.verifier;', label="secondary panel facts verifier dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsBlocker = facts.blocker;', label="secondary panel facts blocker dataset")
    require(render_js, 'dom.secondarySessionFacts.dataset.secondaryFactsFollow = facts.follow;', label="secondary panel facts follow dataset")
    require(render_js, "syncAutonomyDetailSurface", label="secondary autonomy surface sync helper")
    require(render_js, "deriveSelectedThreadLiveAutonomy", label="selected-thread live autonomy render helper import")
    require(render_js, 'autonomyCard.hidden = suppressed;', label="secondary autonomy card healthy suppression")
    require(render_js, 'autonomyCard.dataset.autonomySurface = suppressed ? "suppressed" : "secondary-detail";', label="autonomy card surface dataset")
    require(render_js, 'autonomyCard.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="autonomy card center-timeline authority dataset")
    require(render_js, 'autonomyCard.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="autonomy card center-timeline presentation dataset")
    require(render_js, 'dom.autonomyDetail.dataset.surface = suppressed ? "suppressed" : "secondary-detail";', label="autonomy detail surface dataset")
    require(render_js, 'dom.autonomyDetail.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="autonomy detail center-timeline authority dataset")
    require(render_js, 'dom.autonomyDetail.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="autonomy detail center-timeline presentation dataset")
    require(render_js, "syncExecutionStatusSurface", label="secondary execution surface sync helper")
    require(render_js, 'statusCard.hidden = suppressed;', label="secondary execution card healthy suppression")
    require(render_js, 'statusCard.dataset.executionSurface = suppressed ? "suppressed" : "secondary-detail";', label="execution card surface dataset")
    require(render_js, 'statusCard.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="execution card center-timeline authority dataset")
    require(render_js, 'statusCard.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="execution card center-timeline presentation dataset")
    require(render_js, 'dom.statusOutput.dataset.surface = suppressed ? "suppressed" : "secondary-detail";', label="status output surface dataset")
    require(render_js, 'dom.statusOutput.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="status output center-timeline authority dataset")
    require(render_js, 'dom.statusOutput.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="status output center-timeline presentation dataset")
    require(render_js, 'dom.jobEvents.dataset.surface = suppressed ? "suppressed" : "secondary-detail";', label="job events surface dataset")
    require(render_js, 'dom.jobEvents.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="job events center-timeline authority dataset")
    require(render_js, 'dom.jobEvents.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="job events center-timeline presentation dataset")
    require(render_js, 'dom.jobPhase.dataset.surface = suppressed ? "suppressed" : "secondary-detail";', label="job phase surface dataset")
    require(render_js, 'dom.jobPhase.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="job phase center-timeline authority dataset")
    require(render_js, 'dom.jobPhase.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="job phase center-timeline presentation dataset")
    require(render_js, 'dom.jobMeta.dataset.surface = suppressed ? "suppressed" : "secondary-detail";', label="job meta surface dataset")
    require(render_js, 'dom.jobMeta.dataset.centerTimelineAuthority = timelineAuthority.visible ? "true" : "false";', label="job meta center-timeline authority dataset")
    require(render_js, 'dom.jobMeta.dataset.centerTimelinePresentation = timelineAuthority.presentation;', label="job meta center-timeline presentation dataset")
    require(store_js, "autonomySummary: null,", label="autonomy summary state cache")
    require(app_js, "buildAutonomySummary", label="autonomy summary helper import")
    require(conversations_js, "buildAutonomySummary", label="autonomy summary helper dependency")
    require(conversations_js, "state.autonomySummary = normalizeAutonomySummary(", label="autonomy summary state hydration")
    require(conversations_js, "state.autonomySummary = null;", label="autonomy summary clear state")
    require(render_js, "dataset.sessionPresentation", label="session strip presentation dataset")
    require(render_js, "dataset.sessionTerminal", label="session strip terminal dataset")
    require(render_js, "sessionRail", label="session rail local expansion state")
    require(render_js, "transportChip", label="composer state transport chip helper")
    require(render_js, "phaseChip", label="composer state phase chip helper")
    require(render_js, "proposalChip", label="composer state proposal chip helper")
    require(render_js, "phaseDetailHint", label="phase detail hint helper")
    require(render_js, "shouldShowComposerLiveStrip", label="composer live strip visibility helper")
    require(render_js, "selectedThreadInlineSessionState", label="inline session visibility helper")
    require(render_js, "selectedThreadTimelineAuthorityModel", label="timeline authority model helper")
    require(render_js, 'authority.state === "provisional"', label="provisional authority presentation branch")
    require(render_js, "const degradedVisible =", label="inline degraded visibility helper")
    require(render_js, "const provisionalVisible = sessionStatus.presentation === \"provisional\";", label="inline provisional visibility helper")
    require(render_js, 'sessionIndicator.state === "reconnecting" || sessionIndicator.state === "polling"', label="inline degraded state scope")
    require(render_js, "if (inlineState.visible) {", label="composer strip suppression guard")
    require(render_js, "renderInlineSessionBlock", label="inline session workspace block helper")
    require(render_js, 'data-selected-thread-live-block="true"', label="inline session live block dataset")
    require(render_js, 'data-live-block-provisional="${provisionalVisible ? "true" : "false"}"', label="inline session provisional dataset")
    require(render_js, 'data-live-block-conversation-id="${escapeHtml(String(sessionStatus.conversationId || ""))}"', label="inline session block conversation dataset")
    require(render_js, 'data-live-block-phase="${escapeHtml(phaseLabel)}"', label="inline session block phase dataset")
    require(render_js, 'data-live-block-transport="${escapeHtml(transportLabel)}"', label="inline session block transport dataset")
    require(render_js, 'if (handoffVisible || inlineState.liveVisible) {', label="transcript live activity suppression behind inline session block")
    require(render_js, "INLINE_TERMINAL_RETENTION_MS = 12000", label="inline terminal retention window")
    require(render_js, "shouldRetainInlineTerminalPhase", label="inline terminal retention helper")
    require(render_js, "const { handoffVisible, degradedVisible, sessionIndicator } = inlineState;", label="transcript live state wiring")
    require(conversations_js, "function normalizeSessionStatus(payload = {}, overrides = {}) {", label="session-status normalizer helper")
    require(conversations_js, 'state.appendStream.sessionStatus = normalizeSessionStatus(appendEnvelope.session_status || {}, {', label="append envelope session-status hydration")
    require(conversations_js, 'state.appendStream.sessionStatus = normalizeSessionStatus(payload.session_status || {}, {', label="bootstrap session-status hydration")
    require(conversations_js, 'goalTitle: String(payload?.goal_title || payload?.goalTitle || "Autonomy Goal"),', label="session-status goal title normalization")
    require(conversations_js, 'goalStatus: String(payload?.goal_status || payload?.goalStatus || "unknown"),', label="session-status goal status normalization")
    require(conversations_js, 'iteration: String(payload?.iteration || ""),', label="session-status iteration normalization")
    require(conversations_js, 'heading: String(payload?.heading || "").trim(),', label="session-status heading normalization")
    require(conversations_js, 'freshnessState: String(payload?.freshness_state || payload?.freshnessState || "stale-or-missing").toLowerCase(),', label="session-status freshness normalization")
    require(conversations_js, 'fallbackAllowed: Boolean(payload?.fallback_allowed ?? payload?.fallbackAllowed ?? true),', label="session-status fallback normalization")
    require(conversations_js, 'transportState: "polling-fallback"', label="polling fallback session-status transport label")
    require(conversations_js, 'transportState: "reconnecting"', label="reconnecting session-status transport label")
    require(store_js, "export function deriveSelectedThreadSessionStripModel(currentState, conversation = null, liveRun = null) {", label="selected-thread session strip model helper")
    require_absent(render_js, 'data-live-session-strip="true"', label="inline selected-thread session strip DOM")
    require_absent(render_js, 'data-session-event-source="session-status"', label="canonical inline session-event source dataset")
    require_absent(render_js, 'if (sessionStrip.visible) {', label="transcript live activity suppression behind session strip")
    require(api_runtime_context_py, "def conversation_session_status(self, conversation_id: str, payload: dict[str, Any] | None, *, kind: str) -> dict[str, Any]:", label="runtime canonical session-status helper")
    require(api_runtime_context_py, '"session_status": self.conversation_session_status(conversation_id, payload, kind=kind),', label="append envelope session-status emission")
    require(api_runtime_context_py, '"goal_title": str(autonomy_summary.get("goal_title") or autonomy_summary.get("goalTitle") or "Autonomy Goal"),', label="runtime session-status goal title emission")
    require(api_runtime_context_py, '"goal_status": str(autonomy_summary.get("goal_status") or autonomy_summary.get("goalStatus") or "unknown"),', label="runtime session-status goal status emission")
    require(api_runtime_context_py, '"iteration": str(autonomy_summary.get("iteration") or ""),', label="runtime session-status iteration emission")
    require(api_runtime_context_py, '"heading": str(', label="runtime session-status heading emission")
    require(api_runtime_context_py, '"freshness_state": str(', label="runtime session-status freshness emission")
    require(api_runtime_context_py, '"fallback_allowed": bool(', label="runtime session-status fallback emission")
    require(render_js, 'if (item.pending_assistant && inlineState.handoffVisible) {', label="pending assistant placeholder suppression behind inline block")
    require(render_js, 'data-live-reason="${escapeHtml(', label="transcript live reason dataset")
    require(render_js, 'const retainedTerminalVisible = shouldRetainInlineTerminalPhase(', label="inline terminal retention wiring")
    require(render_js, '(!liveRun.terminal || retainedTerminalVisible);', label="inline terminal visibility guard")
    require(render_js, 'Date.now() - createdAtMs <= INLINE_TERMINAL_RETENTION_MS;', label="inline terminal retention deadline")
    require(render_js, "(liveRun.phase !== \"READY\" && liveRun.phase !== \"APPLIED\")", label="inline terminal phase scope")
    require(render_js, "latestAppendId > terminalAppendId", label="inline terminal next-append clear guard")
    require(render_js, "renderTranscriptLiveActivity", label="transcript live activity helper")
    require(render_js, "selectedThreadPrimaryTimelineSessionModel", label="primary selected-thread timeline session helper")
    require(render_js, "const { liveAutonomy, phaseProgression, milestoneModel } = sessionSurface;", label="transcript live activity shared session surface destructuring")
    require(render_js, "renderTranscriptMilestones", label="transcript live milestone helper")
    require(render_js, 'data-live-milestones="true"', label="transcript live milestone dataset")
    require(render_js, 'data-live-milestones-explicit="true"', label="explicit transcript live milestone lane dataset")
    require(render_js, 'data-milestone-key="${escapeHtml(item.key)}"', label="transcript milestone key dataset")
    require(render_js, 'data-milestone-state="${escapeHtml(item.state)}"', label="transcript milestone state dataset")
    require(render_js, 'data-live-session-event="${liveOwned ? "true" : "false"}"', label="transcript session event lane dataset")
    require(render_js, 'data-live-session-duplicates="${collapseSessionEvents ? "collapsed" : "allowed"}"', label="transcript session-event collapse dataset")
    require(render_js, 'data-live-session-lane="${escapeHtml(liveOwned ? "selected-thread" : degradedVisible ? "degraded" : handoffVisible ? "handoff" : "fallback")}"', label="transcript session lane ownership dataset")
    require(render_js, 'data-live-milestones-visible="${liveOwned && milestoneModel.visible ? "true" : "false"}"', label="transcript session event milestone visibility dataset")
    require(render_js, 'data-live-milestones-phase="${escapeHtml(liveOwned ? String(milestoneModel.currentLabel || phaseLabel) : "")}"', label="transcript session event milestone phase dataset")
    require(render_js, 'data-live-path-verdict="${escapeHtml(pathVerdict)}"', label="transcript session lane path verdict dataset")
    require(render_js, 'data-live-expected-path="${escapeHtml(expectedPath)}"', label="transcript session lane expected-path dataset")
    require(render_js, 'data-live-verifier-acceptability="${escapeHtml(verifierAcceptability)}"', label="transcript session lane verifier dataset")
    require(render_js, 'data-live-blocker-reason="${escapeHtml(blockerReason)}"', label="transcript session lane blocker dataset")
    require(render_js, 'data-live-transport="${escapeHtml(transportLabel)}"', label="transcript session lane transport dataset")
    require(render_js, 'data-live-transport-owned="${liveOwned ? "true" : "false"}"', label="transcript session lane transport ownership dataset")
    require(render_js, 'data-live-session-primary="true"', label="transcript primary session surface dataset")
    require(render_js, "const transcriptLiveActivity = renderTranscriptLiveActivity(conversation, currentState, liveRun);", label="transcript live activity wiring")
    require(render_js, 'data-thread-transition-transport="${escapeHtml(transportLabel)}"', label="restore transition transport dataset")
    require(render_js, "sessionTimelineEventModel", label="session timeline event model helper")
    require(render_js, "renderSessionTimelineEvent", label="session timeline event render helper")
    require(render_js, "shouldCollapseSelectedThreadSessionEvent", label="selected-thread transcript session-event collapse helper")
    require(render_js, 'data-session-event="true"', label="session timeline event DOM")
    require(render_js, 'data-session-phase="${escapeHtml(model.phase)}"', label="session timeline event phase dataset")
    require(render_js, 'data-session-milestone="${escapeHtml(model.milestone)}"', label="session timeline event milestone dataset")
    require(render_js, 'data-session-verdict="${escapeHtml(model.verdict.toLowerCase())}"', label="session timeline event verdict dataset")
    require(render_js, "const timelineSession = selectedThreadPrimaryTimelineSessionModel(conversation, currentState, liveRun);", label="selected-thread transcript collapse shared model wiring")
    require(render_js, "const { collapseSessionEvents } = timelineSession;", label="selected-thread transcript collapse state destructuring")
    require(render_js, "if (shouldCollapseSelectedThreadSessionEvent(item, currentState, conversation, liveRun)) {", label="selected-thread transcript session-event suppression guard")
    require(render_js, "collapseSessionEvents: liveOwned,", label="healthy selected-thread session-event collapse ownership")
    require(render_js, 'const sessionEvent = renderSessionTimelineEvent(item);', label="session timeline event projection wiring")
    require(render_js, "if (sessionEvent) {", label="session timeline event branch")
    require(render_js, "transcriptLiveTone", label="transcript live activity tone helper")
    require(render_js, "composerActionHint", label="composer affordance hint helper")
    require(render_js, "threadMetaSummary", label="thread meta phase summary helper")
    require(render_js, "sessionProvenance", label="session provenance helper")
    require(render_js, "toggleSessionRail", label="session rail toggle helper")
    require(render_js, 'dom.secondarySessionFacts.hidden = facts.presentation === "suppressed";', label="secondary facts healthy suppression")
    require(render_js, 'dom.threadSessionSummary.dataset.threadSummaryPresentation = summaryVisible ? badgePresentation : "cleared";', label="thread summary presentation dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.threadSummaryOwner = summaryVisible ? summaryOwner : "";', label="thread summary owner dataset")
    require(render_js, 'dom.threadSessionSummary.dataset.threadSummaryReason = summaryVisible ? badgeReason : "idle";', label="thread summary reason dataset")
    require(render_js, 'dom.threadSessionSummaryOwner.textContent = summaryOwner;', label="thread summary owner chip copy")
    require(render_js, 'dom.threadPhaseChip.hidden = true;', label="header phase badge hidden behind capsule")
    require(render_js, "dataset.sessionCollapsed", label="session strip collapsed dataset")
    require(render_js, 'if (!conversationId && !(threadTransition.active && threadTransition.targetConversationId) && !sessionStatus.conversationId) {', label="composer strip idle clear branch")
    require(render_js, 'const ownerState = composerOwnerState(currentState, conversation);', label="composer strip owner helper wiring")
    require(render_js, 'const transportState = composerTransportState(currentState, conversation, liveRun, handoffState);', label="composer strip transport helper wiring")
    require(render_js, 'const footerDock = selectedThreadFooterDockModel(currentState, conversation, liveRun, footerFollow);', label="composer strip footer dock helper wiring")
    require(render_js, 'dom.sessionStrip.hidden = !sessionConversationId;', label="composer strip visibility")
    require(render_js, 'dom.sessionStrip.dataset.restoreStage = sessionStatus.restoreStage || "none";', label="composer strip restore stage dataset")
    require_absent(render_js, "if (footerDock.visible) {", label="legacy healthy footer-dock transcript suppression guard")
    require(render_js, 'dom.sessionStrip.dataset.restorePath = sessionStatus.restorePath || "none";', label="composer strip restore path dataset")
    require(render_js, 'dom.sessionStrip.dataset.restoreProvenance = sessionStatus.restoreProvenance || "none";', label="composer strip restore provenance dataset")
    require(render_js, "const liveOwned =", label="composer live strip live ownership guard")
    require(render_js, "detail: provisionalVisible", label="footer dock detail model")
    require(render_js, "const provisionalPhaseLabel = String(", label="footer dock provisional phase label helper")
    require(render_js, "const provisionalOwnerLabel = String(authority.ownerLabel || \"ATTACH\").toUpperCase();", label="footer dock provisional transport label helper")
    require(render_js, 'label: provisionalOwnerLabel, tone: "neutral", role: "live-owner"', label="footer dock provisional owner chip")
    require(render_js, 'label: provisionalPhaseLabel, tone: "neutral", role: "live-phase"', label="footer dock provisional phase chip")
    require(render_js, "joinSessionChromeTokens(provisionalOwnerLabel, provisionalPhaseLabel)", label="footer dock provisional minimal detail copy")
    require(render_js, 'joinSessionChromeTokens(phaseLabel, proposalLabel, runStateLabel)', label="footer dock phase-specific detail copy")
    require(render_js, "const summaryMeta = provisionalVisible", label="inline provisional summary copy branch")
    require(render_js, 'joinSessionChromeTokens("selected thread", transportLabel, phaseLabel)', label="inline provisional minimal meta copy")
    require(render_js, 'dom.sessionStrip.dataset.liveOwned = stripProgressOwned ? "true" : "false";', label="composer live strip ownership dataset")
    require(render_js, 'dom.sessionStrip.dataset.sessionOwner = selectedFooterLaneVisible ? "selected-thread" : "none";', label="composer live strip selected-thread owner dataset")
    require(render_js, 'dom.sessionStrip.dataset.sessionPresentation = presentation;', label="composer strip presentation dataset")
    require(render_js, 'dom.sessionStrip.dataset.composerTransportOwned = stripLiveOwned ? "true" : "false";', label="composer strip transport ownership dataset")
    require(render_js, 'dom.sessionStrip.dataset.followState = footerFollow.visible ? footerFollow.followState : stripLiveOwned ? sessionStatus.followState || "live" : transportState.owned ? "owned" : "idle";', label="composer strip follow-state dataset")
    require(render_js, 'dom.sessionStrip.dataset.followCount = String(footerFollow.visible ? footerFollow.unseenCount : 0);', label="composer strip follow-count dataset")
    require(render_js, 'dom.sessionStrip.dataset.footerDockOwned = stripProgressOwned ? "true" : "false";', label="composer strip footer-dock ownership dataset")
    require(render_js, 'dom.sessionStrip.dataset.footerDockPhase = footerDock.phaseLabel || "IDLE";', label="composer strip footer-dock phase dataset")
    require(render_js, 'dom.sessionStrip.dataset.footerDockSource = footerDock.source || "none";', label="composer strip footer-dock source dataset")
    require(render_js, "milestoneVisible: liveOwned && chips.length > 1,", label="footer dock milestone visibility helper")
    require(render_js, 'dom.sessionStrip.dataset.footerDockMilestones = footerDock.milestoneVisible ? "true" : "false";', label="composer strip footer-dock milestone dataset")
    require(render_js, 'dom.composerUtilityMenu.dataset.composerUtilityOpen = "false";', label="composer utility closed on non-dock render")
    require(render_js, 'dom.composerUtilityCluster.hidden = true;', label="composer utility cluster hidden on non-dock render")
    require(render_js, 'dom.composerUtilityToggle.setAttribute("aria-expanded", "false");', label="composer utility aria closes on non-dock render")
    require(render_js, 'dom.threadScroller.dataset.sessionOwner = liveOwned ? "selected-thread" : "none";', label="thread scroller selected-thread owner dataset")
    require(render_js, 'dom.sessionStripState.hidden = false;', label="composer strip state visible on healthy path")
    require(render_js, 'dom.sessionStripState.dataset.sessionStripRole = stripState.role;', label="composer strip state role dataset")
    require(render_js, 'dom.sessionStripState.dataset.sessionStripLabel = stripState.label;', label="composer strip state label dataset")
    require(render_js, 'dom.sessionStripState.dataset.sessionStripTone = stripState.tone;', label="composer strip state tone dataset")
    require(render_js, 'dom.sessionStripMeta.hidden = false;', label="composer strip target visibility")
    require(render_js, 'dom.sessionStripMeta.textContent = ownerState.target;', label="composer strip target copy")
    require(render_js, 'dom.sessionStripState.innerHTML = sessionStripStateChipMarkup(stripState.chips || stripState);', label="composer strip chip render")
    require(render_js, 'dom.sessionStripDetail.hidden = false;', label="composer strip detail visibility")
    require(render_js, 'dom.sessionStripDetail.textContent = footerDock.visible', label="composer strip footer-dock detail copy")
    require(render_js, 'reason: "thread-switch"', label="composer strip switch reason")
    require(store_js, 'transportReason = sessionRotationDetected ? "session-rotation" : retrying ? "retrying" : "polling-fallback";', label="composer strip polling fallback reason")
    require(render_js, "session-chip", label="session chip render markup")
    require(render_js, "function isSessionAuthorityEvent(event)", label="selected-thread session authority event helper")
    require(render_js, "function selectedThreadSseAuthorityEvent(conversation, currentState)", label="selected-thread SSE authority event selector")
    require(render_js, "function selectedThreadSseAuthorityStatus(conversation, currentState)", label="selected-thread SSE authority status helper")
    require(render_js, "function sessionAuthorityJobId(conversation, currentState)", label="selected-thread authority job id helper")
    require(render_js, "function sessionAuthorityEvents(conversation, currentState)", label="selected-thread authority event filter helper")
    require(render_js, 'String(event?.delivery_source || "").toLowerCase() !== "sse"', label="selected-thread SSE event provenance gate")
    require(render_js, "export function renderJobActivity(dom, conversation, currentJobId, jobPayload = null, currentState = null)", label="job activity current-state signature")
    require(render_js, "const selectedThreadSseOwned =", label="job activity selected-thread SSE ownership guard")
    require(render_js, "const liveRun = currentState ? deriveLiveRunState(conversation, currentState)", label="job activity live-run ownership source")
    require(render_js, "const jobId = sessionAuthorityJobId(conversation, currentState);", label="live run session authority job id")
    require(render_js, "const relevantEvents = sessionAuthorityEvents(conversation, currentState);", label="live run session authority event filter")
    require(render_js, "const liveAuthorityEvent = selectedThreadSseAuthorityEvent(conversation, currentState);", label="selected-thread authority event lookup")
    require(render_js, 'return liveAuthorityEvent;', label="healthy selected-thread authority event return")
    require(render_js, "const eventJobId = currentState", label="job activity event job id authority switch")
    require(render_js, "sessionAuthorityJobId(conversation, currentState)", label="job activity session authority job id")
    require(render_js, "const phase = selectedThreadSseOwned", label="job activity phase ownership switch")
    require(render_js, 'String(liveRun.phase || "IDLE").toUpperCase()', label="job activity SSE phase label")
    require(render_js, 'phaseLabel(jobPayload?.status || latestEvent?.status || "", latestEvent?.type || "")', label="job activity polling fallback phase label")
    require(render_js, "currentState.latestProposalJobId =", label="apply readiness from selected-thread live state")
    require(render_js, "const sessionStrip = deriveSelectedThreadSessionStripModel(currentState, conversation, liveRun);", label="selected-thread session strip apply authority")
    require(render_js, 'sessionStrip?.owned && (sessionStrip.proposalReady || sessionStrip.proposalStatus === "ready_to_apply")', label="session-strip proposal readiness apply guard")
    require(render_js, "updateProposalButton(dom, currentState.latestProposalJobId);", label="apply button updated from selected-thread live state")
    require(render_js, 'renderJobActivity(dom, conversation, currentState.currentJobId || conversation.latest_job_id || "", null, currentState);', label="selected conversation job activity uses current state")
    require(render_js, "headerPhaseTone", label="thread header phase tone helper")
    require(render_js, "threadTitle", label="thread title header wiring")
    require(render_js, "threadSessionSummary", label="thread session summary wiring")
    require(render_js, 'return { label: "SEND", tone: "thinking" };', label="compact send transport chip")
    require(render_js, 'return { label: "ACCEPTED", tone: "neutral" };', label="compact accepted handoff chip")
    require(render_js, 'label: transportState.label,', label="compact reconnect transport chip")
    require(render_js, 'return { label: "OPEN", tone: "neutral" };', label="compact connecting transport chip")
    require(render_js, 'return { label: "OFFLINE", tone: "danger" };', label="compact offline transport chip")
    require(render_js, 'return { label: "READY", tone: "healthy" };', label="compact ready chip")
    require(render_js, 'return { label: "APPLIED", tone: "healthy" };', label="compact applied chip")
    require(render_js, 'return { label: "BLOCKED", tone: "warning" };', label="compact blocked proposal chip")
    require(render_js, 'return "복구 중";', label="compact reconnect action hint")
    require(render_js, 'return "복구 필요";', label="compact offline action hint")
    require(render_js, 'return "연결 중";', label="compact connecting action hint")
    require(render_js, 'return "전송 중";', label="compact sending action hint")
    require(render_js, 'return "handoff 완료";', label="compact accepted action hint")
    require(render_js, 'return "HYPOTHESIS";', label="phase detail proposal hint")
    require(render_js, 'return "REVIEW SIGNAL";', label="phase detail review hint")
    require(render_js, 'return "VERIFY SIGNAL";', label="phase detail verify hint")
    require(render_js, 'return "AUTO APPLY";', label="phase detail auto-apply hint")
    require(render_js, 'return "READY TO APPLY";', label="phase detail proposal-ready hint")
    require(render_js, 'return "APPLY COMPLETE";', label="phase detail applied hint")
    require(render_js, 'return "응답 생성";', label="compact generating action hint")
    require(render_js, 'return "실행 중";', label="compact live execution action hint")
    require(render_js, 'return "계속 입력";', label="compact live action hint")
    require(render_js, 'return "입력 가능";', label="compact idle action hint")
    require(render_js, '"RECONNECT"', label="degraded stream provenance label")
    require(render_js, '"OPEN"', label="connecting stream provenance label")
    require(render_js, '"OFFLINE"', label="offline stream provenance label")
    require(render_js, 'dom.sessionStripToggle.hidden = !footerFollow.visible;', label="session strip toggle follow visibility")
    require(render_js, 'dom.sessionStripToggle.dataset.sessionAction = footerFollow.visible ? "jump-latest" : "toggle-session-rail";', label="session strip toggle action dataset")
    require(render_js, 'dom.sendRequestButton.textContent = status === "live" || presentation === "sending" ? "추가 지시 보내기" : "메시지 보내기"', label="contextual send copy")
    require(render_js, 'liveRun.state === "proposal-ready" || authority.phaseLabel === "READY" ? "지금 제안 적용" : "제안 적용"', label="contextual apply copy")
    require(render_js, "dataset.liveRunState", label="live run state dataset")
    require(render_js, "dataset.liveRunPhase", label="live run phase dataset")
    require(render_js, "dataset.liveRunSource", label="live run source dataset")
    require(render_js, "dataset.followState", label="follow-state dataset")
    require(render_js, 'data-live-activity-turn="true"', label="transcript live activity DOM")
    require(render_js, 'data-live-owned="${liveOwned ? "true" : "false"}"', label="transcript live activity ownership DOM")
    require(render_js, 'data-live-autonomy-presentation="${escapeHtml(liveAutonomy.presentation)}"', label="transcript live activity autonomy presentation DOM")
    require(render_js, 'data-live-autonomy-presentation="${escapeHtml(degradedVisible ? "degraded" : handoffVisible ? "handoff" : liveAutonomy.presentation)}"', label="transcript live activity unified autonomy presentation DOM")
    require(render_js, 'data-append-source="sse-live-activity"', label="transcript live activity provenance DOM")
    require(render_js, '${milestoneLane}', label="transcript live activity milestone projection")
    require(render_js, 'timeline-live-row timeline-live-row-milestones', label="transcript milestone row class")
    require(render_js, '<span class="timeline-live-chip" data-tone="neutral">SELECTED</span>', label="transcript live rail selected scope chip")
    require(render_js, '${escapeHtml(transportLabel)}', label="transcript live rail transport owner chip")
    require(render_js, "selected thread", label="transcript live activity selected-thread copy")
    require(render_js, "!handoffVisible && !degradedVisible && (!phaseProgression.visible || !liveAutonomy.visible)", label="transcript live activity unified visibility guard")
    require(render_js, 'const phaseLabel = degradedVisible', label="transcript live activity unified phase label")
    require(render_js, 'String(sessionSurface.phaseLabel || phaseProgression.label || liveRun.phase || "LIVE").toUpperCase();', label="transcript live activity shared phase label wiring")
    require(render_js, 'const provenanceLabel = degradedVisible', label="transcript live activity unified provenance label")
    require(render_js, 'const pathVerdict = liveOwned ? sessionSurface.pathVerdict : "";', label="transcript live activity shared path verdict wiring")
    require(render_js, 'const verifierAcceptability = liveOwned ? sessionSurface.verifierAcceptability : "";', label="transcript live activity shared verifier wiring")
    require(render_js, 'const blockerReason = liveOwned ? sessionSurface.blockerReason : "";', label="transcript live activity shared blocker wiring")
    require(store_js, "deriveSelectedThreadFollowControlModel", label="selected-thread follow control model helper")
    require(render_js, "selectedThreadFooterFollowState", label="footer follow state helper")
    require(render_js, "syncJumpToLatest", label="jump-to-latest sync helper")
    require(render_js, "updateLiveFollowFromScroll", label="live follow scroll helper")
    require(render_js, "jumpToLatest", label="jump-to-latest action helper")
    require(render_js, "currentState.liveFollow", label="live follow state wiring")
    require(render_js, "lastSeenAppendId", label="live follow seen append cursor")
    require(render_js, "pendingAppendCount", label="live follow unseen append count")
    require(render_js, "followRenderSource", label="jump-to-latest render source dataset")
    require(render_js, "footerFollowActionLabel", label="footer follow action label helper")
    require(render_js, 'const footerFollow = selectedThreadFooterFollowState(dom, currentState, conversationId, renderSource);', label="follow control footer follow source")
    require(render_js, 'dom.sessionStripToggle.textContent = footerFollowActionLabel(footerFollow);', label="footer follow action label render")
    require(render_js, 'dom.sessionStripToggle.dataset.followRenderSource = footerFollow.renderSource || renderSource || "snapshot";', label="footer follow render source dataset")
    require(store_js, 'followState === "new"', label="follow control new-state derivation")
    require(store_js, 'followState === "paused"', label="follow control paused-state derivation")
    require(store_js, "새 live append", label="follow control new copy")
    require(store_js, "live follow paused · unseen", label="follow control paused copy")
    require(render_js, 'dom.sessionStripToggle.hidden = !footerFollow.visible;', label="footer bar follow action visibility")
    require(render_js, 'dom.sessionStripToggle.dataset.sessionAction = footerFollow.visible ? "jump-latest" : "toggle-session-rail";', label="footer bar jump-latest action")
    require(render_js, "data-append-source", label="append provenance attribute")
    require(render_js, "dataset.streamState = status", label="stream-state dataset")
    require(render_js, "dataset.attachMode = attachMode", label="attach-mode dataset")
    require(render_js, "dataset.bootstrapVersion = bootstrapVersion", label="bootstrap-version dataset")
    require(render_js, "dataset.resumeMode = resumeMode", label="resume-mode dataset")
    require(render_js, "dataset.resumeCursor = String(resumeCursor)", label="resume-cursor dataset")
    require(render_js, "dataset.phaseValue", label="phase value dataset")
    require(render_js, "dataset.phaseAuthoritative", label="phase authoritative dataset")
    require(render_js, "dataset.phaseProvenance", label="phase provenance dataset")
    require(render_js, 'ownerState.state === "switching" ? "UNKNOWN"', label="switching phase value reset")
    require(render_js, '"thread-transition"', label="switching phase provenance")
    require(render_js, "dataset.renderSource = lastRenderSource", label="render-source dataset")
    require(conversations_js, 'dom.threadScroller.dataset.pendingConversationId = conversationId', label="thread switch pending marker")
    require(conversations_js, "state.currentConversationId && state.currentConversationId !== conversationId", label="thread switch clear guard")
    require(store_js, "threadTransition", label="thread transition state")
    require(store_js, "appendStream", label="append stream state")
    require(store_js, "pendingOutgoing", label="pending outgoing state")
    require(store_js, "assistantCreatedAt", label="assistant placeholder pending timestamp state")
    require(store_js, "baselineAppendId", label="pending handoff baseline append id state")
    require(store_js, "isAppendStreamConnected", label="append stream connection helper")
    require(store_js, "liveFollow", label="live follow state")
    require(store_js, "lastSeenAppendId", label="live follow seen append state")
    require(store_js, "pendingAppendCount", label="live follow unseen append state")
    require(index_html, 'id="session-strip"', label="session strip DOM")
    require(index_html, 'data-composer-shell="stable-session-dock"', label="stable composer shell DOM")
    require(index_html, 'id="session-strip-state"', label="session strip state DOM")
    require(index_html, 'id="session-strip-meta"', label="session strip meta DOM")
    require(index_html, 'id="session-strip-detail"', label="session strip detail DOM")
    require(index_html, 'id="composer-owner-row"', label="composer owner row DOM")
    require(index_html, 'id="composer-owner-state"', label="composer owner state DOM")
    require(index_html, 'id="composer-owner-target"', label="composer owner target DOM")
    require(index_html, 'id="composer-owner-copy"', label="composer owner copy DOM")
    require(index_html, 'data-thread-header="conversation"', label="conversation-first thread header DOM")
    require(index_html, 'id="thread-title"', label="thread title DOM")
    require(index_html, 'id="thread-phase-chip"', label="legacy thread phase chip DOM")
    require(index_html, 'id="thread-session-summary"', label="thread session summary DOM")
    require(index_html, 'id="thread-session-summary-scope"', label="thread session summary scope DOM")
    require(index_html, 'id="thread-session-summary-path"', label="thread session summary path DOM")
    require(index_html, 'id="thread-session-summary-owner"', label="thread session summary owner DOM")
    require(index_html, 'id="thread-session-summary-phase"', label="thread session summary phase DOM")
    require_absent(index_html, 'id="session-summary-row"', label="removed session summary row DOM")
    require_absent(index_html, 'id="recent-thread-rail"', label="removed recent-thread rail DOM")
    require_absent(index_html, 'id="recent-thread-rail-list"', label="removed recent-thread rail list DOM")
    require_absent(index_html, 'id="session-live-indicator"', label="removed header live-session indicator DOM")
    require_absent(index_html, 'id="session-summary-scope"', label="removed session summary scope DOM")
    require_absent(index_html, 'id="session-summary-path"', label="removed session summary path DOM")
    require_absent(index_html, 'id="session-summary-state"', label="removed session summary state DOM")
    require_absent(index_html, 'id="session-summary-copy"', label="removed session summary copy DOM")
    require(index_html, 'id="session-strip-toggle"', label="session strip toggle DOM")
    require(index_html, 'data-activity-bar="composer-adjacent"', label="composer-adjacent activity bar DOM")
    require(index_html, 'class="composer-toolbar"', label="chat-style composer toolbar DOM")
    require(index_html, 'class="composer-send-primary"', label="primary send button DOM")
    require(index_html, "composer-secondary-action", label="secondary composer action DOM")
    require(index_html, 'id="composer-utility-menu"', label="composer utility menu DOM")
    require(index_html, 'id="composer-utility-toggle"', label="composer utility toggle DOM")
    require(index_html, 'id="composer-utility-cluster"', label="composer utility cluster DOM")
    require(index_html, 'data-composer-layout="chat-first"', label="chat-first composer footer DOM")
    require(index_html, 'data-composer-utility-open="false"', label="collapsed composer utility default DOM")
    require(index_html, 'data-composer-utility-state="closed"', label="closed composer utility default DOM")
    require(index_html, 'data-composer-utility-panel="compact"', label="compact composer utility panel DOM")
    require(index_html, 'aria-controls="composer-utility-cluster"', label="composer utility toggle controls DOM")
    require(index_html, 'aria-hidden="true"', label="collapsed composer utility aria hidden DOM")
    require(index_html, 'class="toggle inline composer-utility-toggle"', label="composer utility toggle DOM")
    require(index_html, 'id="conversation-footer-dock"', label="conversation footer dock DOM")
    require(index_html, 'id="active-session-row"', label="active session row DOM")
    require(index_html, 'id="active-session-owner"', label="active session owner DOM")
    require(index_html, 'id="active-session-state"', label="active session state DOM")
    require(index_html, 'id="active-session-follow"', label="active session follow DOM")
    require(index_html, 'id="active-session-title"', label="active session title DOM")
    require(index_html, 'id="active-session-meta"', label="active session meta DOM")
    require(index_html, 'data-active-session-owned="false"', label="active session owned default")
    require(index_html, 'data-active-session-source="none"', label="active session source default")
    require(index_html, 'data-active-session-phase="IDLE"', label="active session phase default")
    require(index_html, 'data-active-session-unseen-count="0"', label="active session unseen default")
    require(index_html, 'data-footer-dock="session-composer"', label="footer dock mode DOM")
    require(index_html, 'data-session-workspace="conversation-first"', label="conversation-first session workspace DOM")
    require_absent(index_html, 'id="jump-to-latest"', label="removed floating jump-to-latest DOM")
    require(index_html, 'id="autonomy-detail"', label="secondary panel autonomy detail DOM")
    require(index_html, 'id="autonomy-detail-meta"', label="secondary panel autonomy detail meta DOM")
    require(index_html, 'id="secondary-session-facts"', label="secondary panel session facts DOM")
    require(index_html, 'id="secondary-panel"', label="secondary panel DOM")
    require(index_html, 'data-secondary-panel-mode="compact-sidecar"', label="compact side panel DOM")
    require(index_html, 'id="secondary-panel-toggle"', label="secondary panel toggle DOM")
    require(index_html, 'id="secondary-panel-close"', label="secondary panel close DOM")
    require(index_html, 'id="nav-sheet-toggle"', label="mobile nav toggle DOM")
    require(index_html, 'id="nav-sheet-close"', label="mobile nav close DOM")
    require(index_html, 'id="nav-sheet-scrim"', label="mobile nav scrim DOM")
    require(index_html, 'id="nav-sheet"', label="mobile nav sheet DOM")
    require(index_html, 'data-mobile-surface="sheet"', label="mobile nav sheet surface marker")
    require(index_html, 'data-mobile-nav-priority="conversations"', label="mobile nav conversation priority DOM")
    require(index_html, 'data-nav-primary="threads"', label="mobile nav threads-first section DOM")
    require(index_html, 'data-nav-secondary="ops"', label="mobile nav secondary operator section DOM")
    require(index_html, 'id="selected-app-summary"', label="selected app summary DOM")
    require(index_html, 'data-sidebar-app-summary="compact"', label="compact app summary DOM")
    require(index_html, 'data-primary-surface="conversation"', label="active conversation primary surface DOM")
    require(index_html, 'aria-controls="nav-sheet"', label="mobile nav toggle controls DOM")
    require(index_html, 'aria-expanded="false"', label="mobile nav closed default DOM")
    require(index_html, 'id="nav-ops-section"', label="mobile operator section DOM")
    require(index_html, 'id="nav-ops-summary"', label="mobile operator summary DOM")
    require(index_html, 'id="thread-scroller" class="thread-scroller" tabindex="-1"', label="conversation surface focus target DOM")
    require(app_js, "internalConversationAppendStreamUrl", label="controller dependency wiring")
    require(app_js, "showPendingOutgoing", label="pending outgoing submit wiring")
    require(app_js, "showPendingAssistant", label="pending assistant submit wiring")
    require(app_js, "clearPendingOutgoing", label="pending outgoing clear wiring")
    require(app_js, "isSelectedThreadSessionOwned", label="submit path selected-thread session ownership helper wiring")
    require(app_js, 'if (!isSelectedThreadSessionOwned(state, conversationId)) {', label="submit path degraded-only eager poll guard")
    require(app_js, "} else {\n      jobController.stopPolling();\n    }", label="submit path sse-owned polling suppression")
    require_absent(app_js, "jobController.stopPolling();\n    await jobController.pollCurrentState();\n    jobController.ensurePollingForJob();", label="legacy unconditional eager submit poll")
    require(app_js, "setNavigationOpen", label="mobile nav state helper")
    require(app_js, "isPhoneViewport", label="phone breakpoint helper")
    require(app_js, "focusPrimaryNavAction", label="mobile nav primary action focus helper")
    require(app_js, "returnFocusToConversationSurface", label="mobile nav conversation return focus helper")
    require(app_js, "syncMobileWorkspaceMode", label="mobile nav mode helper")
    require(app_js, 'dom.secondaryPanelToggle.setAttribute("aria-expanded", isOpen ? "true" : "false");', label="secondary panel aria expanded sync")
    require(app_js, 'document.body.dataset.mobileNavMode = isPhoneViewport() ? "drawer" : "desktop"', label="mobile nav mode dataset")
    require(app_js, 'document.body.dataset.mobileWorkspace = isOpen ? "navigation" : "conversation"', label="mobile workspace state helper")
    require(app_js, 'dom.navOpsSection.open = false;', label="mobile operator section collapse on phone")
    require(app_js, 'dom.navSheetToggle.setAttribute("aria-expanded", isOpen ? "true" : "false")', label="mobile nav toggle expanded state")
    require(app_js, 'navSheet.setAttribute("aria-hidden", isOpen ? "false" : "true")', label="mobile nav sheet aria state")
    require(app_js, "syncNavOpsSection", label="mobile operator section state helper")
    require(app_js, "dom.navOpsSection.open = false;", label="operator section collapsed by default")
    require(app_js, 'primaryAction.focus({ preventScroll: true })', label="mobile nav focuses first thread action")
    require(app_js, 'preferredTarget.focus({ preventScroll: true })', label="mobile conversation surface focus return")
    require(app_js, "updateLiveFollowFromScroll", label="live follow scroll event wiring")
    require(app_js, "jumpToLatest", label="footer follow jump action wiring")
    require(app_js, 'if (dom.sessionStripToggle?.dataset.sessionAction === "jump-latest") {', label="session strip toggle jump-latest wiring")
    require(app_js, "reengageLiveFollowFromComposer", label="composer reengages live follow helper")
    require(app_js, 'dom.requestTextInput.addEventListener("focus", reengageLiveFollowFromComposer)', label="composer focus reengages live follow")
    require(app_js, 'dom.requestTextInput.addEventListener("input", reengageLiveFollowFromComposer)', label="composer input reengages live follow")
    require(app_js, "toggleSessionRail", label="session rail toggle wiring")
    require_absent(app_js, 'dom.recentThreadRailList?.addEventListener("click", async (event) => {', label="removed recent-thread rail click wiring")
    require(app_js, "setSecondaryPanelOpen", label="secondary panel state helper")
    require(app_js, "setComposerUtilityOpen", label="composer utility state helper")
    require(app_js, "syncSendButtonState", label="composer send availability helper")
    require(app_js, 'dom.sendRequestButton.dataset.sendBusy = "true";', label="composer send busy start")
    require(app_js, 'dom.sendRequestButton.dataset.sendBusy = "false";', label="composer send busy end")
    require(app_js, 'dom.sendRequestButton?.dataset.composerBlocked === "true"', label="composer blocked guard")
    require(app_js, 'const utilityOpen = isOpen ? "true" : "false";', label="composer utility open helper state")
    require(app_js, 'const utilityState = isOpen ? "open" : "closed";', label="composer utility finite helper state")
    require(app_js, 'dom.composerUtilityMenu.dataset.composerUtilityOpen = utilityOpen;', label="composer utility menu open dataset")
    require(app_js, 'dom.composerUtilityMenu.dataset.composerUtilityState = utilityState;', label="composer utility menu finite dataset")
    require(app_js, 'dom.composerUtilityCluster.dataset.composerUtilityOpen = utilityOpen;', label="composer utility cluster open dataset")
    require(app_js, 'dom.composerUtilityCluster.dataset.composerUtilityState = utilityState;', label="composer utility cluster finite dataset")
    require(app_js, 'dom.composerUtilityCluster.hidden = !isOpen;', label="composer utility hidden state")
    require(app_js, 'dom.composerUtilityCluster.setAttribute("aria-hidden", isOpen ? "false" : "true");', label="composer utility cluster aria state")
    require(app_js, 'dom.composerUtilityToggle.dataset.composerUtilityOpen = utilityOpen;', label="composer utility toggle open dataset")
    require(app_js, 'dom.composerUtilityToggle.dataset.composerUtilityState = utilityState;', label="composer utility toggle finite dataset")
    require(app_js, 'dom.composerUtilityToggle.setAttribute("aria-expanded", utilityOpen);', label="composer utility aria state")
    require_absent(app_js, 'dom.composerUtilityMenu.dataset.composerUtilityOpen = isOpen ? "true" : "false";', label="legacy composer utility inline dataset state")
    require_absent(app_js, 'dom.composerUtilityToggle.setAttribute("aria-expanded", isOpen ? "true" : "false");', label="legacy composer utility inline aria state")
    require(app_js, 'persistSettings();\n  setComposerUtilityOpen(false);\n  dom.sendRequestButton.dataset.sendBusy = "true";', label="composer utility closes on send transition")
    require(app_js, 'dom.appSelect.addEventListener("change", async () => {\n    setComposerUtilityOpen(false);', label="composer utility closes on app change")
    require(app_js, 'dom.newConversationButton.addEventListener("click", async () => {\n    setComposerUtilityOpen(false);', label="composer utility closes on new conversation")
    require(app_js, 'setComposerUtilityOpen(false);\n    state.savedConversationId = button.dataset.conversationId || "";\n    await conversationController.handleConversationChange();', label="composer utility closes on selected-thread change")
    require(app_js, 'window.innerWidth > 860', label="mobile nav resize reset")
    require(deployed_console_sh, 'WORKSPACE_APP_ID="${WORKSPACE_APP_ID:-factory-runtime}"', label="workspace gate app default")
    require(deployed_console_sh, 'python3 ./scripts/verify_deployed_workspace_gate.py', label="deployed workspace gate wiring")
    require(api_js, 'sessionId: option.dataset.sessionId || ""', label="selected app session id API wiring")
    require(store_js, "appSession", label="selected app session state")
    require(deployed_workspace_py, 'DEFAULT_REQUEST_TEXT = (', label="workspace gate default request")
    require(deployed_workspace_py, 'goal.proposal.phase.started', label="workspace gate proposal phase assertion")
    require(deployed_workspace_py, 'goal.review.phase.started', label="workspace gate review phase assertion")
    require(deployed_workspace_py, 'goal.verify.phase.started', label="workspace gate verify phase assertion")
    require(deployed_workspace_py, "proposal.ready", label="workspace gate proposal ready assertion")
    require(deployed_workspace_py, 'conversation.append', label="workspace gate sse append assertion")
    require(deployed_workspace_py, "phaseChip", label="workspace gate browser-visible phase chip assertion")
    require(deployed_workspace_py, "dataset.liveRunPhase", label="workspace gate live phase dataset assertion")
    require(deployed_workspace_py, "dataset.streamState", label="workspace gate transport dataset assertion")
    require(deployed_workspace_py, "dataset.attachMode", label="workspace gate attach-mode dataset assertion")
    require(deployed_workspace_py, "dataset.bootstrapVersion", label="workspace gate bootstrap-version dataset assertion")
    require(deployed_workspace_py, "load_playwright", label="workspace gate playwright loader")
    require(deployed_workspace_py, "assert_browser_runtime_surface", label="workspace gate browser runtime helper")
    require(deployed_workspace_py, "browser_snapshot_script", label="workspace gate browser snapshot helper")
    require(deployed_workspace_py, 'VERIFY_BROWSER_RUNTIME', label="workspace gate browser runtime env toggle")
    require(deployed_workspace_py, "__verifyDelayedConversationId", label="workspace gate delayed conversation fetch helper")
    require(deployed_workspace_py, "__verifyDelayedConversationConsumed", label="workspace gate delayed conversation fetch consumption guard")
    require(deployed_workspace_py, "__verifyConversationDelayMs", label="workspace gate delayed conversation fetch timeout")
    require(deployed_workspace_py, 'page.fill("#request-text", request_text)', label="workspace gate browser composer submission")
    require(deployed_workspace_py, 'window.__verifyForceDegrade = true;', label="workspace gate browser degrade trigger")
    require(deployed_workspace_py, "inlineBlocks.length === 0", label="workspace gate browser no inline session block assertion")
    require(deployed_workspace_py, 'data-thread-transition="switching"', label="workspace gate browser switch placeholder assertion")
    require(deployed_workspace_py, '.timeline-empty', label="workspace gate browser empty-state selector")
    require(deployed_workspace_py, "#thread-scroller", label="workspace gate browser thread scroller assertion")
    require(deployed_workspace_py, 'sessionStripToggle.dataset.sessionAction === "jump-latest"', label="workspace gate browser footer follow action assertion")
    require(deployed_workspace_py, '["new", "paused"].includes(sessionStripToggle.dataset.followState || "")', label="workspace gate browser footer follow state assertion")
    require(deployed_workspace_py, 'page.click("#session-strip-toggle")', label="workspace gate browser footer jump-latest action")
    require(deployed_workspace_py, 'sessionStripToggle.dataset.sessionAction === "toggle-session-rail"', label="workspace gate browser footer follow clear action assertion")
    require(deployed_workspace_py, "document.querySelectorAll('[data-thread-transition=\"switching\"]').length === 1", label="workspace gate browser single switch placeholder assertion")
    require(deployed_workspace_py, "__verifyStartSwitchMonitor", label="workspace gate browser switch monitor start helper")
    require(deployed_workspace_py, "__verifySwitchMonitor", label="workspace gate browser switch monitor state")
    require(deployed_workspace_py, "switchMonitor.sawEmptyState === false", label="workspace gate browser no empty-state flash assertion")
    require(deployed_workspace_py, "switchMonitor.maxTransitionCount === 1", label="workspace gate browser single compact placeholder assertion")
    require(deployed_workspace_py, "switchMonitor.sawHiddenComposerDock === false", label="workspace gate browser composer dock continuity assertion")
    require(deployed_workspace_py, "switchMonitor.sawClearedWorkspacePlaceholder === false", label="workspace gate browser no premature placeholder clear assertion")
    require(deployed_workspace_py, 'threadScroller.dataset.sessionOwner !== "selected-thread"', label="workspace gate browser clears old live ownership on switch")
    require(deployed_workspace_py, 'follow.dataset.followOwned !== "selected-thread"', label="workspace gate browser clears follow ownership on switch")
    require(deployed_workspace_py, 'threadPhase.dataset.liveSessionOwned === "false"', label="workspace gate browser clears header ownership on switch")
    require(deployed_workspace_py, 'const selectedCardLiveOwnerRow = selectedCard ? selectedCard.querySelector(\'[data-conversation-live-owner-row]\') : null;', label="workspace gate browser selected row live owner selector")
    require(deployed_workspace_py, 'selectedCardLiveOwnerRow.dataset.liveOwnerVisible === "true"', label="workspace gate browser healthy selected row live marker assertion")
    require(deployed_workspace_py, 'selectedCardLiveOwnerRow.dataset.liveOwnerShadow === "true"', label="workspace gate browser healthy selected row shadow parity assertion")
    require(deployed_workspace_py, 'selectedCardLiveOwnerRow.dataset.liveOwnerVisible === "false"', label="workspace gate browser cleared selected row live marker assertion")
    require(deployed_workspace_py, 'executionStatusCard.hidden', label="workspace gate browser hides execution card on healthy path")
    require(deployed_workspace_py, 'executionStatusCard.dataset.executionSurface === "center-lane"', label="workspace gate browser center-lane execution surface")
    require(deployed_workspace_py, 'statusOutput.dataset.surface === "center-lane"', label="workspace gate browser status output center-lane surface")
    require(deployed_workspace_py, 'sessionStrip.dataset.liveOwned === "true"', label="workspace gate browser active composer strip ownership assertion")
    require(deployed_workspace_py, 'sessionStrip.dataset.sessionOwner === "selected-thread"', label="workspace gate browser active composer strip session owner assertion")
    require(deployed_workspace_py, 'sessionStrip.dataset.sessionPresentation !== "suppressed"', label="workspace gate browser active composer strip presentation assertion")
    require(deployed_workspace_py, 'sessionStrip.dataset.footerDockOwned === "true"', label="workspace gate browser active composer strip footer ownership assertion")
    require(deployed_workspace_py, 'sessionStrip.dataset.composerTransportOwned === "true"', label="workspace gate browser active composer strip transport ownership assertion")
    require(deployed_workspace_py, 'sessionStrip.dataset.phaseProvenance === "sse"', label="workspace gate browser composer strip phase provenance assertion")
    require(deployed_workspace_py, 'threadScroller.dataset.phaseProvenance === "sse"', label="workspace gate browser thread scroller phase provenance assertion")
    require(deployed_workspace_py, "goalsFetches.length === 0", label="workspace gate browser no goals fetch on healthy path")
    require(deployed_workspace_py, 'terminal_event = "proposal.ready"', label="workspace gate proposal-ready terminal attribution")
    require_absent(deployed_workspace_py, "wait_for_job(", label="removed deployed workspace job wait helper")
    require_absent(deployed_workspace_py, 'payload.get("latest_job_id"', label="removed deployed workspace latest job id wait loop")
    require(deployed_workspace_py, 'executionStatusCard.hidden', label="workspace gate browser keeps execution card hidden on degraded path")
    require(deployed_workspace_py, 'executionStatusCard.dataset.executionSurface === "center-lane"', label="workspace gate browser degraded execution center-lane surface")
    require(deployed_workspace_py, 'statusOutput.dataset.surface === "center-lane"', label="workspace gate browser degraded status output center-lane surface")
    require(deployed_workspace_py, 'entry => String(entry.url || "").includes("/api/jobs/")', label="workspace gate browser switch job fetch selector")
    require(deployed_workspace_py, "jobFetches.length === 0", label="workspace gate browser no job fetch on switch")
    require(deployed_workspace_py, 'threadPhase.hidden', label="workspace gate browser hides single header badge on switch")
    require(deployed_workspace_py, 'composerOwnerRow.hidden', label="workspace gate browser switching composer owner merged")
    require(deployed_workspace_py, 'composerOwnerRow.dataset.composerOwnerMerged === "true"', label="workspace gate browser healthy or switching composer owner merged dataset")
    require(deployed_workspace_py, 'composerOwnerRow.dataset.composerOwnerMerged === "false"', label="workspace gate browser idle composer owner merged dataset")
    require(deployed_workspace_py, 'inlineBlocks.length === 0', label="workspace gate browser removes healthy inline block on switch")
    require(deployed_workspace_py, 'window.__verifyDelayedConversationId = String(switchConversationId || "");', label="workspace gate browser delayed switch setup")
    require(deployed_workspace_py, 'transition.dataset.threadTransitionConversationId === targetConversationId', label="workspace gate browser cancelled switch retarget assertion")
    require(deployed_workspace_py, 'activeSessionRow.dataset.activeSessionSource === "thread-transition"', label="workspace gate browser active session switch source assertion")
    require(deployed_workspace_py, 'activeSessionRow.dataset.activeSessionState === "switching"', label="workspace gate browser active session switch state assertion")
    require(deployed_workspace_py, 'activeSessionRow.dataset.activeSessionFollow === "attach"', label="workspace gate browser active session switch follow assertion")
    require(deployed_workspace_py, 'item.event === "session.bootstrap"', label="workspace gate browser bootstrap event assertion")
    require(deployed_workspace_py, 'conversationFetches.length === 0', label="workspace gate browser no snapshot fetch assertion")
    require(deployed_workspace_py, "selectedThreadLiveSessionIndicator", label="workspace gate header live-session helper assertion")
    require(deployed_workspace_py, "threadPhase.dataset.liveSessionVisible", label="workspace gate single header badge dataset assertion")
    require(deployed_workspace_py, "threadSessionSummary.dataset.threadSummaryVisible", label="workspace gate thread summary dataset assertion")
    require(api_runtime_context_py, '"autonomy_summary": dict(conversation.get("autonomy_summary") or {})', label="bootstrap autonomy summary envelope")
    require(api_runtime_context_py, 'conversation["autonomy_summary"] = self.autonomy_summary_payload(', label="conversation snapshot autonomy summary envelope")
    require(api_runtime_context_py, '"freshness_state": "stale-or-missing"', label="autonomy stale-or-missing marker")
    require(api_runtime_context_py, '"fallback_allowed": fallback_allowed,', label="autonomy fallback allowed field")
    require(api_runtime_context_py, '"source": source,', label="autonomy summary source field")
    require(api_runtime_context_py, '"generated_at": generated_at,', label="autonomy summary generated-at field")
    require(api_routes_py, "return context.conversation_snapshot_payload(conversation_id)", label="conversation snapshot route autonomy envelope")
    require(deployed_workspace_py, 'codex.exec.retrying', label="workspace gate retry degradation assertion")
    require(deployed_workspace_py, "unexpected session rotation", label="workspace gate session rotation guard")
    require(styles_css, 'body[data-nav-open="true"] .sidebar', label="mobile nav drawer CSS")
    require(styles_css, 'body[data-mobile-nav-mode="drawer"] .sidebar[data-mobile-nav-priority="conversations"] .sidebar-primary-nav', label="mobile nav drawer mode CSS")
    require(styles_css, 'body[data-mobile-workspace="conversation"] .main-stage', label="conversation-first mobile surface CSS")
    require(styles_css, "touch-action: none;", label="mobile nav open touch lock CSS")
    require(styles_css, 'body[data-secondary-panel-open="true"] .desktop-shell', label="desktop secondary panel layout CSS")
    require(styles_css, "grid-template-columns: 15rem minmax(0, 1fr);", label="desktop two-pane shell width")
    require(styles_css, "grid-template-columns: 13.5rem minmax(0, 1fr);", label="desktop two-pane shell width at medium desktop")
    require(styles_css, ".autonomy-detail-card", label="secondary panel autonomy detail card CSS")
    require(styles_css, ".secondary-panel", label="secondary panel CSS")
    require(styles_css, ".secondary-session-facts", label="secondary panel session facts CSS")
    require(styles_css, ".secondary-session-chip-row", label="secondary panel session chip row CSS")
    require(styles_css, "position: fixed;", label="desktop secondary panel overlay positioning")
    require(styles_css, "width: min(18.5rem, calc(100vw - 2rem));", label="desktop secondary panel overlay width")
    require(styles_css, ".thread-title-row", label="thread title row CSS")
    require_absent(styles_css, ".session-summary-row", label="removed session summary row CSS")
    require_absent(styles_css, ".recent-thread-rail", label="removed recent-thread rail CSS")
    require_absent(styles_css, ".recent-thread-rail-list", label="removed recent-thread rail list CSS")
    require_absent(styles_css, ".recent-thread-chip", label="removed recent-thread chip CSS")
    require_absent(styles_css, ".session-live-indicator", label="removed header live-session indicator CSS")
    require_absent(styles_css, ".session-summary-copy", label="removed session summary copy CSS")
    require_absent(styles_css, '.session-summary-row[data-summary-path="switching"] .session-summary-chip[data-summary-chip="path"]', label="removed summary switching chip CSS")
    require_absent(styles_css, '.session-summary-row[data-summary-path="handoff"] .session-summary-chip[data-summary-chip="path"]', label="removed summary handoff chip CSS")
    require_absent(styles_css, '.session-summary-row[data-summary-path="sse"] .session-summary-chip[data-summary-chip="path"]', label="removed summary sse chip CSS")
    require(styles_css, ".thread-phase-chip", label="legacy thread phase chip CSS")
    require(styles_css, ".thread-session-summary", label="thread session summary CSS")
    require(styles_css, '.thread-session-summary[data-thread-summary-presentation="healthy"] .session-summary-chip[data-thread-summary-chip="owner"]', label="thread session summary owner CSS")
    require(styles_css, ".thread-sidecar", label="thread sidecar CSS")
    require(styles_css, "width: min(100%, 54rem);", label="dominant conversation column width")
    require(styles_css, ".conversation-card-live", label="conversation card live badge CSS")
    require(styles_css, ".conversation-card-marker", label="conversation card selected badge CSS")
    require(styles_css, ".conversation-card-session", label="conversation card session badge CSS")
    require(styles_css, ".conversation-card-tags", label="conversation card session tag row CSS")
    require(styles_css, ".conversation-card-preview", label="conversation card preview CSS")
    require(styles_css, ".active-session-row", label="active session row CSS")
    require(styles_css, "position: sticky;", label="active session sticky position CSS")
    require(styles_css, ".active-session-chip", label="active session chip CSS")
    require(styles_css, ".active-session-title", label="active session title CSS")
    require(styles_css, ".active-session-meta", label="active session meta CSS")
    require(styles_css, ".timeline-transition-target", label="thread transition compact target CSS")
    require(styles_css, '.active-session-row[data-active-session-state="paused"] .active-session-chip[data-active-chip="state"]', label="active session paused chip CSS")
    require(styles_css, '.active-session-row[data-active-session-follow="live"] .active-session-chip[data-active-chip="follow"]', label="active session live follow chip CSS")
    require(styles_css, '.active-session-row[data-active-session-follow="attach"] .active-session-chip[data-active-chip="follow"]', label="active session attach follow chip CSS")
    require(styles_css, '.conversation-card-preview[data-preview-lines="1"]', label="single-line conversation preview CSS")
    require(styles_css, '.conversation-card[data-thread-state="active"] .conversation-card-live', label="active card state CSS")
    require(styles_css, '.conversation-card[data-thread-state="active"] .conversation-card-session', label="active session marker CSS")
    require(styles_css, '.conversation-card[data-thread-state="review"] .conversation-card-live', label="review card state CSS")
    require(styles_css, '.conversation-card[data-thread-state="verify"] .conversation-card-live', label="verify card state CSS")
    require(styles_css, '.conversation-card[data-thread-state="ready"] .conversation-card-live', label="ready card state CSS")
    require(styles_css, '.conversation-card[data-live-owner-state="handoff"] .conversation-card-marker', label="selected handoff owner marker CSS")
    require(styles_css, '.conversation-card[data-live-owner-state="new"] .conversation-card-marker', label="selected new owner marker CSS")
    require(styles_css, '.conversation-card[data-live-owner-state="paused"] .conversation-card-marker', label="selected paused owner marker CSS")
    require_absent(conversations_js, "메시지 handoff 중", label="legacy selected row handoff prose")
    require_absent(conversations_js, "첫 응답 대기", label="legacy selected row accepted prose")
    require_absent(conversations_js, "선택된 세션 연결 중", label="legacy selected row connecting prose")
    require_absent(conversations_js, "선택된 세션 복구 중", label="legacy selected row reconnect prose")
    require(styles_css, '.conversation-card[data-thread-state="failed"] .conversation-card-live', label="failed card state CSS")
    require(styles_css, '.conversation-card[data-thread-state="idle"] .conversation-card-live', label="idle card state CSS")
    require(styles_css, ".timeline-item.message.user.pending-local", label="pending local message CSS")
    require(styles_css, ".timeline-item.message.assistant.pending-assistant", label="pending assistant message CSS")
    require(styles_css, ".timeline-item.live-activity", label="transcript live activity CSS")
    require(styles_css, ".session-inline-block", label="inline session block CSS")
    require(styles_css, ".session-inline-chip", label="inline session chip CSS")
    require(styles_css, ".session-inline-meta", label="inline session meta CSS")
    require(styles_css, ".timeline-live-row", label="transcript live activity chip row CSS")
    require(styles_css, ".timeline-item.session-event", label="session timeline event CSS")
    require(styles_css, '.timeline-item.session-event[data-session-verdict="disqualifying"]', label="session timeline disqualifying CSS")
    require(styles_css, ".timeline-live-chip", label="transcript live activity chip CSS")
    require(styles_css, ".session-strip", label="session strip CSS")
    require(styles_css, "min-height: 4.75rem;", label="stable session strip minimum height")
    require(styles_css, "flex-wrap: nowrap;", label="stable session strip no-wrap layout")
    require(styles_css, "text-overflow: ellipsis;", label="stable composer status truncation")
    require(styles_css, 'grid-template-rows: minmax(4.75rem, auto) auto minmax(4.1rem, auto) auto;', label="stable composer shell row layout")
    require(styles_css, "min-height: 15rem;", label="stable composer shell minimum height")
    require(styles_css, ".composer-toolbar {\n  min-height: 1.9rem;", label="stable composer toolbar minimum height")
    require(styles_css, ".composer-footer {\n  align-items: flex-end;\n  min-height: 2.9rem;", label="stable composer footer minimum height")
    require(styles_css, ".composer-owner-row", label="composer owner row CSS")
    require(styles_css, ".composer-owner-chip", label="composer owner chip CSS")
    require(styles_css, ".composer-owner-target", label="composer owner target CSS")
    require(styles_css, '.composer-send-primary[data-composer-blocked="true"]', label="composer blocked send CSS")
    require(styles_css, '.session-strip[data-live-owned="true"]', label="composer live strip active CSS")
    require(styles_css, '.session-strip[data-stream-state="reconnecting"]', label="composer reconnecting strip CSS")
    require(styles_css, '.session-strip[data-stream-state="offline"]', label="composer offline strip CSS")
    require(styles_css, '.session-strip[data-stream-state="connecting"]', label="composer connecting strip CSS")
    require(styles_css, ".session-activity-bar", label="composer activity bar CSS")
    require(styles_css, ".session-chip", label="composer state chip CSS")
    require(styles_css, "flex-wrap: wrap;", label="composer activity inline wrap CSS")
    require(styles_css, "justify-content: flex-start;", label="composer activity inline alignment CSS")
    require(styles_css, ".session-strip-toggle {\n  min-width: 5.8rem;\n  margin-left: auto;", label="composer activity toggle push CSS")
    require(styles_css, ".session-strip-meta,\n.session-strip-detail {\n  color: var(--muted);", label="composer activity inline metadata CSS")
    require(styles_css, "display: inline-flex;", label="composer activity inline chip display CSS")
    require_absent(styles_css, ".session-strip-main", label="legacy composer activity main CSS")
    require_absent(styles_css, ".session-strip-side", label="legacy composer activity side CSS")
    require(render_js, 'const phaseLabel = degradedVisible', label="transcript degraded phase mapping")
    require(render_js, "const renderedItems = items", label="transcript render item join")
    require(render_js, 'if (!items.length && !inlineSessionBlock && !transcriptLiveActivity) {', label="transcript empty-state live activity guard")
    require(render_js, 'dom.conversationTimeline.innerHTML = inlineSessionBlock + renderedItems + transcriptLiveActivity;', label="canonical inline session lane timeline ordering")
    require_absent(render_js, 'dom.conversationTimeline.innerHTML = inlineSessionBlock + items', label="legacy inline block prepended render")
    require_absent(render_js, "In Flight Assistant", label="duplicate accepted status block copy")
    require_absent(render_js, 'return { label: "GENERATING", tone: "neutral" };', label="legacy generating handoff chip")
    require(styles_css, ".session-strip-draft", label="composer activity draft CSS")
    require(styles_css, ".composer-toolbar", label="chat-style composer toolbar CSS")
    require(styles_css, ".composer-footer", label="chat-first composer footer CSS")
    require(styles_css, ".composer-send-primary", label="primary send button CSS")
    require(styles_css, ".composer-utility-menu", label="composer utility menu CSS")
    require(styles_css, ".composer-utility-toggle-button", label="composer utility toggle button CSS")
    require(styles_css, ".composer-secondary-action", label="secondary composer action CSS")
    require(styles_css, ".composer-utility-cluster", label="composer utility cluster CSS")
    require(styles_css, ".composer-utility-toggle", label="composer utility toggle CSS")
    require(styles_css, ".composer-hint-compact", label="compact composer hint CSS")
    require(styles_css, ".sidebar-primary-nav", label="mobile conversation-first nav section CSS")
    require(styles_css, ".sidebar-app-summary", label="compact app summary CSS")
    require(styles_css, ".sidebar-app-summary-copy", label="compact app summary copy CSS")
    require(styles_css, ".sidebar-ops-details", label="mobile operator section CSS")
    require(styles_css, ".nav-ops-summary", label="mobile operator summary CSS")
    require(styles_css, ".nav-ops-content", label="mobile operator content CSS")
    require(styles_css, '.sidebar[data-mobile-nav-priority="conversations"] .sidebar-primary-nav[data-nav-primary="threads"]', label="mobile nav primary section priority CSS")
    require(styles_css, '.sidebar[data-mobile-nav-priority="conversations"] .sidebar-ops-details[data-nav-secondary="ops"]', label="mobile nav secondary section CSS")
    require(styles_css, ".sidebar-ops-details[open] .nav-ops-summary::after", label="mobile operator summary state CSS")
    require(styles_css, "order: 1;", label="conversation rail primary order CSS")
    require(styles_css, "order: 2;", label="operator rail secondary order CSS")
    require_absent(styles_css, ".jump-to-latest", label="removed floating jump-to-latest CSS")
    require(styles_css, ".timeline-transition", label="thread transition CSS")
    require(styles_css, ".timeline-transition-chip", label="thread transition chip CSS")
    require(styles_css, "position: sticky;", label="sticky footer CSS")
    require(styles_css, "env(safe-area-inset-bottom)", label="safe-area footer padding CSS")
    require(styles_css, ".mobile-nav-toggle", label="mobile nav button CSS")
    require(styles_css, ".thread-scroller:focus-visible", label="conversation surface focus ring CSS")
    require_absent(index_html, 'id="conversation-live-status"', label="legacy live badge DOM")
    require_absent(index_html, 'id="live-run-row"', label="legacy live run row DOM")
    require_absent(index_html, 'id="append-stream-strip"', label="legacy append stream DOM")
    require_absent(index_html, 'id="autonomy-context-strip"', label="header autonomy strip DOM")
    require_absent(index_html, 'id="hero-conversation-state"', label="header conversation status DOM")
    require_absent(index_html, 'id="hero-job-state"', label="header job status DOM")
    require_absent(index_html, 'id="hero-app-name"', label="legacy app-centric header title DOM")
    require_absent(index_html, "<h3>메시지 보내기</h3>", label="prose-heavy composer heading DOM")
    require_absent(index_html, "sidebar-inspector", label="legacy sidebar inspector")
    require_absent(index_html, "sidebar-footer", label="legacy sidebar footer")
    require_absent(index_html, 'class="autonomy-rail"', label="legacy standalone autonomy rail DOM")
    print("ok: ops desktop secondary panel, footer dock, session strip, append SSE wiring, and mobile nav drawer are present")
    return 0


if __name__ == "__main__":
    try:
        raise SystemExit(main())
    except AssertionError as error:
        print(f"verification failed: {error}", file=sys.stderr)
        raise SystemExit(1)
