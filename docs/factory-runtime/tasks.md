# Factory Runtime Tasks

## Iteration 145

- [x] Reuse existing selected-thread shell-phase data to make the healthy header chip phase-first instead of generic ownership-first.
- [x] Keep compact ownership provenance in the existing header copy without adding another chip or prose surface.
- [x] Make the composer-adjacent live-follow chip reuse the explicit selected-thread phase label instead of generic `LIVE` or `READY`.
- [x] Publish machine-readable header indicator phase and detail datasets for verification.
- [x] Align focused static and deployed verifiers plus proposal artifacts with the iteration-145 phase-specific session-chrome contract.
