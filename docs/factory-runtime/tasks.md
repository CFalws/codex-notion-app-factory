# Factory Runtime Tasks

- [x] Keep the current selected-conversation SSE path, transcript shell, footer composer dock, sticky active-session row, and side-panel behavior unchanged.
- [x] Promote the selected-thread healthy-session state into the composer-adjacent live strip so the active conversation itself carries the primary live execution signal.
- [x] Keep that strip chip-first and selected-thread scoped by reusing the existing selected-thread ownership helper instead of inventing another status source.
- [x] Hide the strip immediately on reconnect downgrade, polling fallback, thread switch, or terminal completion so degraded or stale live-owned UI never remains near the composer.
- [x] Align the focused verifier, deployed workspace gate, and iteration artifacts with the healthy-path-only live strip contract.
