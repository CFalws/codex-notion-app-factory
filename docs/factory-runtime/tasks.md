# Factory Runtime Tasks

- [x] Keep the current selected-conversation SSE path, transcript shell, footer composer dock, sticky active-session row, and side-panel behavior unchanged.
- [x] Reuse the existing conversation message and event history instead of introducing another preview or state source.
- [x] Expand the non-selected snapshot label vocabulary so waiting, active, ready, done, failed, and idle states remain distinguishable without making any non-selected row live-owned.
- [x] Keep one bounded preview line per row and prefer recent assistant or user message text over event prose whenever message content exists.
- [x] Keep the focused verifier and iteration artifacts aligned with the compact snapshot-label and preview contract.
