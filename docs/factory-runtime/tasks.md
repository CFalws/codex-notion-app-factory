# Factory Runtime Tasks

## Iteration 81

- [x] Reuse the existing selected-thread SSE ownership, `liveRun`, and phase datasets for the composer-adjacent strip.
- [x] Collapse the healthy selected-thread strip state to one compact phase chip.
- [x] Relabel the same strip state row to `RECONNECT` or `POLLING` on degraded ownership.
- [x] Clear stale healthy phase copy on switch and idle through transition or cleared strip state.
- [x] Align focused browser-proof verifiers and iteration artifacts with the strip single-chip phase contract.
