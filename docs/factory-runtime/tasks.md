# Factory Runtime Tasks

- [x] Choose one bounded blocker-precedence hypothesis instead of broader proposal/apply policy changes.
- [x] Make verifier `path_acceptability=disqualifying` outrank `proposal_ready` in `continuation_blocker_reason()`.
- [x] Keep proposal/apply behavior unchanged so only the canonical blocker explanation changes.
- [x] Verify both the disqualifying proposal-ready case and the healthy proposal-ready case in the runtime contract.
- [x] Update the `factory-runtime` docs to match the current runtime iteration.
