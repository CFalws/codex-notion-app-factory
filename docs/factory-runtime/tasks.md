# Factory Runtime Tasks

- [x] Keep the current selected-conversation SSE path, compact composer row, left rail markers, pending-turn handoff, and central timeline unchanged.
- [x] Keep only thread identity and navigation affordances in the desktop active-pane header.
- [x] Remove stale header-only autonomy and status render hooks that no longer have DOM ownership.
- [x] Leave fuller autonomy and operator detail in the existing secondary panel instead of the header path.
- [x] Extend focused verification so future sessions can prove the active-pane header stays minimal and conversation-first.
