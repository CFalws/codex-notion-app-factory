# Factory Runtime Tasks

- [x] Keep the current selected-conversation SSE path, session strip ownership, bottom follow control, footer composer, and non-selected snapshot rows unchanged.
- [x] Restore the conversation list as the first visible rail section on desktop and phone widths.
- [x] Add one compact always-visible selected-app summary near the rail header so the current app stays identifiable without opening heavier controls.
- [x] Collapse the app or operator controls section by default so it stays reachable but no longer outranks thread history.
- [x] Extend focused verification so future sessions can prove the conversation-first rail order and compact selected-app summary remain intact.
