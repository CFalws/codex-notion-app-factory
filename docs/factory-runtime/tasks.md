# Factory Runtime Tasks

- [x] Keep the current selected-thread session summary surface, transcript timeline, composer dock, recent-thread rail, and side-panel behavior unchanged.
- [x] Reuse the existing selected-thread live-session mapping as the only header ownership source.
- [x] Render `SSE OWNER` for the healthy selected-thread SSE path while preserving `RECONNECT` and `POLLING` degraded states.
- [x] Publish machine-readable ownership, source, and reason datasets directly on the header ownership chip.
- [x] Align the focused verifier, deployed workspace gate, and iteration artifacts with the selected-thread header ownership contract.
