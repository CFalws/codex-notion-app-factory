# Factory Runtime Tasks

- [x] Reuse the existing selected-thread append SSE transport and healthy workspace datasets as the only authority for the sticky rail row.
- [x] Update the sticky active-session row to mirror compact owner, phase, and follow or unseen chips from the selected-thread SSE path.
- [x] Clear the rail row’s healthy-owned treatment immediately on degraded, idle, terminal, or switched-away states.
- [x] Preserve degraded and switched states as non-owned transport states instead of healthy live progression.
- [x] Align the focused verifier, deployed workspace gate, and iteration artifacts with the active-session SSE mirror contract.
