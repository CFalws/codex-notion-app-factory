# Factory Runtime Tasks

## Iteration 118

- [x] Move transcript-bottom follow ownership onto a store-owned selected-thread follow-control model.
- [x] Keep the transcript-bottom control as the only detached follow surface with explicit `NEW` or `PAUSED` plus unseen-count metadata.
- [x] Preserve immediate clearing on jump-to-latest, scroll re-engagement, switching, reconnect, polling fallback, terminal idle, and non-selected contexts.
- [x] Align focused verifiers and proposal artifacts with the iteration-118 detached follow contract.
