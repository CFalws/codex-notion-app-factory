# Factory Runtime Tasks

- [x] Keep the current selected-conversation SSE path, transcript shell, footer composer dock, sticky active-session row, and side-panel behavior unchanged.
- [x] Reuse the existing compact utility toggle and explicit open or closed dataset instead of introducing another footer state source.
- [x] Preserve the textarea and primary send action as the dominant footer controls while keeping apply and auto-open reachable only through the compact utility cluster.
- [x] Keep the footer utility cluster collapsed by default on desktop and phone widths without adding a second live-status surface or expanding into a form-like footer layout.
- [x] Keep the focused verifier and iteration artifacts aligned with the compact footer utility contract.
