# Factory Runtime Tasks

- [x] Reuse the existing selected-thread `liveRun` mapping as the healthy-path phase authority.
- [x] Surface explicit phase detail in the header badge state without introducing a new live-status model.
- [x] Add an explicit phase chip and concise phase detail copy to the composer-adjacent strip on the healthy SSE path.
- [x] Preserve degraded and switched states as non-owned transport states instead of healthy live progression.
- [x] Align the focused verifier, deployed workspace gate, and iteration artifacts with the explicit-phase presentation contract.
