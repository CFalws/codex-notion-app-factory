# Factory Runtime Tasks

- [x] Reuse the existing selected-thread `liveRun` mapping as the healthy-path phase authority.
- [x] Compress the header summary row and composer owner row to chip-first owner, target, and readiness language.
- [x] Keep the composer-adjacent strip chip-first with explicit healthy phase chips and compact detail labels.
- [x] Preserve degraded and switched states as non-owned transport states instead of healthy live progression.
- [x] Align the focused verifier, deployed workspace gate, and iteration artifacts with the compact healthy-path language contract.
