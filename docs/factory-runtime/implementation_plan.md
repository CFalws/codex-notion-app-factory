# Factory Runtime Implementation Plan

1. Keep the current selected-conversation SSE path, deployed workspace gate, transcript-first center pane, phone conversation-first sheet behavior, and footer live rail unchanged in ownership.
2. Restructure the composer footer so the send action stands alone as the primary conversation control.
3. Move proposal apply and auto-open into a compact secondary utility cluster that stays reachable without competing visually with the textarea.
4. Preserve the same proposal behavior and transcript plus composer reachability on desktop and phone widths while reducing inline control weight.
5. Extend the focused verifier and docs so future sessions can prove the composer remains conversation-first and that utility controls stay secondary.
