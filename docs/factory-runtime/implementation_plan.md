# Factory Runtime Implementation Plan

1. Reuse the existing selected-thread `liveRun` derivation as the only authority for healthy phase labels in the header summary row and composer-adjacent strip.
2. Compress healthy-path summary and owner copy to short owner, target, readiness, and phase language instead of helper prose.
3. Keep the composer-adjacent strip chip-first by adding the live phase chip while using compact phase detail labels instead of sentence-style copy.
4. Preserve degraded reconnect, polling fallback, ownership loss, and thread-switch presentation as explicitly non-owned states.
5. Align the focused verifier, deployed workspace gate, and iteration artifacts with the compact healthy-path language contract.
