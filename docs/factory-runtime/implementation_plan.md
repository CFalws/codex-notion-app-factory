# Factory Runtime Implementation Plan

1. Keep the current selected-conversation SSE path, transcript shell, footer composer dock, sticky active-session row, and side-panel behavior unchanged.
2. Reuse the existing compact utility toggle and explicit open or closed dataset instead of introducing another footer state source.
3. Preserve the textarea and primary send action as the dominant footer controls while keeping apply and auto-open reachable only through the compact utility cluster.
4. Keep the footer utility cluster collapsed by default on desktop and phone widths without adding a second live-status surface or expanding into a form-like footer layout.
5. Keep the focused verifier and durable docs aligned with the compact footer utility contract.
