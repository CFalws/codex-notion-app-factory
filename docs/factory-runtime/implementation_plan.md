# Factory Runtime Implementation Plan

1. Keep the current selected-conversation SSE path, compact composer row, pending-turn handoff, and central timeline unchanged.
2. Narrow the persistent desktop left rail so thread switching stays obvious without competing with the active conversation.
3. Expand the center conversation workspace visually so the transcript, live row, and composer remain the dominant desktop surface.
4. Demote the secondary panel from a third primary column to an overlay drawer so desktop remains a conversation-first two-pane shell by default.
5. Extend the focused verifier and docs so future sessions can prove the desktop shell no longer defaults to a dashboard-like multi-panel layout.
