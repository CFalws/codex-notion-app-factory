# Factory Runtime Implementation Plan

1. Keep the current selected-conversation SSE path, deployed workspace gate, transcript-first center pane, and footer live rail unchanged in ownership.
2. Replace the split session-strip layout with one inline activity bar that keeps transport, phase, proposal, provenance, and action cues in the same compact surface.
3. Preserve the existing machine-readable datasets, phase mappings, and secondary-panel detail path so verification and richer operator context still come from the same owned sources.
4. Keep transcript and composer reachability intact on desktop and phone widths while making the live bar read as one compact conversation-adjacent strip.
5. Extend the focused verifier and docs so future sessions can prove the active workspace stays chip-first and does not regress into a split or prose-heavy strip.
