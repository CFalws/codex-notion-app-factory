# Factory Runtime Implementation Plan

1. Keep the current selected-conversation SSE path, desktop transcript-first layout, left rail markers, pending-turn handoff, and central timeline unchanged.
2. Shorten the composer-adjacent live rail so its primary surface is a fixed chip row for transport, current phase, and proposal readiness.
3. Replace sentence-level strip meta and detail copy with compact provenance and action cues that remain conversation-local to the selected thread.
4. Leave richer autonomy, review, verifier, and operator detail in the existing secondary panel instead of reintroducing prose-heavy in-pane status chrome.
5. Extend the focused verifier and docs so future sessions can prove the selected-thread rail stays chip-first, SSE-driven, and transcript-friendly.
