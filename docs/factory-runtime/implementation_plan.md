# Factory Runtime Implementation Plan

## Iteration 179

Add one healthy selected-thread header session row beside the active conversation title.

1. Keep the change bounded to the existing header summary and phase-badge seam over the selected-thread authority already used in the center pane.
2. Render one compact chip-first summary row for scope, path, ownership, and phase on the healthy selected-thread path only.
3. Keep the existing phase badge as the degraded, reconnect, restore, switch, and no-selection fallback.
4. Preserve machine-readable datasets on the summary row so healthy-path assertions and negative-path clearing stay explicit.
5. Fail closed immediately on degraded, reconnect, polling fallback, restore-gap, deselected, switched, and terminal paths.
6. Align focused verification and proposal artifacts with the iteration-179 header-session contract.
