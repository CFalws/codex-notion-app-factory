# Factory Runtime Implementation Plan

1. Keep the current selected-conversation SSE path, left rail markers, composer-adjacent activity bar, collapsed secondary panel, and mobile drawer unchanged.
2. Reduce the composer shell to a message-first layout by removing prose-heavy heading treatment and compressing helper copy into a lightweight toolbar.
3. Promote send as the primary composer action and demote proposal and auto-open controls so they remain accessible without competing with normal message entry.
4. Preserve clear bottom reachability on desktop and phone widths so the transcript stays dominant while the composer remains continuously usable.
5. Extend the focused verifier and docs so future sessions can prove the composer stays chat-style without reintroducing operator-form framing.
