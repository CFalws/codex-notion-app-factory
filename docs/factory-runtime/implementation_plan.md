# Factory Runtime Implementation Plan

## Iteration 180

Make the healthy selected-thread transcript rail the only readable session summary surface.

1. Keep the change bounded to the existing transcript live-activity plus header and footer summary seams over the selected-thread authority already used in the center pane.
2. Render one compact chip-first transcript rail for scope, transport ownership, phase, path, verifier, blocker, and milestone progression on the healthy selected-thread path only.
3. Hide or neutralize the healthy header row and healthy footer strip when that transcript rail is authoritative.
4. Preserve machine-readable datasets so healthy-path assertions and negative-path clearing stay explicit.
5. Fail closed immediately on degraded, reconnect, polling fallback, restore-gap, deselected, switched, and terminal paths.
6. Align focused verification and proposal artifacts with the iteration-180 transcript-authority contract.
